# Market Lab 第二轮改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 弱化 `tradingDaysPerYear`（按品种自动 + 高级覆盖）、引入人话摘要、整理策略入口（profile 主轴）、精简交互骨架，同时保证 domain 计算层零回归。

**Architecture:** 三层切片 — domain 层新增 `tdpy.js` / `narrative.js` 两个纯函数模块；composable 层让 `usePlanning` 多管 `tdpyOverride`、labStore 集中算 `effectiveTdpy` 注入下游；UI 层删 tdpy 下拉、按 4 个 PR 范围逐个调整组件。

**Tech Stack:** Vue 3 Composition API · Pinia · vitest · `@vue/test-utils` · lightweight-charts · 现有 `usePersisted` localStorage 封装。

**Spec:** `docs/superpowers/specs/2026-05-14-lab-v2-design-cc.md`

**约束**：
- domain 公式数值零回归（`pnpm run verify:domain` 必须过）
- store 对外字段只增不删
- 所有新文件加 `-cc` 后缀仅限 plan/spec 文档；源代码文件不加后缀（项目惯例）
- 中文注释；测试也用中文 describe/it

**PR 划分**：
- **PR-1（任务 1-7）**：日基弱化（tdpy 自动 + 高级设置卡 + 公式参数区去 select）
- **PR-2（任务 8-11）**：图表解释（narrative 模块 + MarketChart 摘要 + DecisionPanel reason）
- **PR-3（任务 12-19）**：交互骨架（QuestionNav 砍 Q3 + ChainFlow 折叠 + ProfileTabs + RightTabs + 顶栏精简）

每个 PR 末尾跑 `pnpm run check:data && pnpm run verify:domain && pnpm run test && pnpm run build`，全过才进入下个 PR。

---

## 任务前置检查

- [ ] **Step 0：确认 git 仓库就绪**

`F:/devarea/market-lab-amplify` 当前不是 git 仓库（`git status` 报 `not a git repository`）。

如果用户要求按 PR 提交，需要先执行：

```bash
cd /f/devarea/market-lab-amplify
git init
git add -A
git commit -m "chore: import current workbench state as baseline"
```

如果用户暂不需要 git，跳过本步并改为：每个任务后用 `pnpm run test` 替代 commit 作为完成检查点。**询问用户偏好后再继续**。

---

## PR-1：日基弱化

### Task 1：`domain/market/tdpy.js` 与单测

**Files:**
- Create: `src/domain/market/tdpy.js`
- Create: `src/domain/__tests__/tdpy.test.js`

- [ ] **Step 1.1：写失败测试**

`src/domain/__tests__/tdpy.test.js`：

```js
import { describe, expect, it } from 'vitest'
import { inferTdpy } from '../market/tdpy.js'

describe('inferTdpy', () => {
  it('加密：market === "加密" → 365 / crypto', () => {
    const meta = inferTdpy({ symbol: 'BTCUSDT', market: '加密' })
    expect(meta).toEqual({ value: 365, basis: 'crypto', label: '加密 365' })
  })

  it('加密：symbol 含 BTC 也走 365 / crypto（兜底）', () => {
    expect(inferTdpy({ symbol: 'BTCUSDT' }).basis).toBe('crypto')
    expect(inferTdpy({ symbol: 'ETHUSDT' }).basis).toBe('crypto')
  })

  it('美股：AAPL → 252 / us', () => {
    const meta = inferTdpy({ symbol: 'AAPL', market: '美股' })
    expect(meta).toEqual({ value: 252, basis: 'us', label: '美股 252' })
  })

  it('港股：0700.HK → 242 / hk', () => {
    expect(inferTdpy({ symbol: '0700.HK', market: '港股' }).value).toBe(242)
    expect(inferTdpy({ symbol: '9988.HK' }).basis).toBe('hk')
  })

  it('A 股：6 位数字 symbol → 242 / cn', () => {
    expect(inferTdpy({ symbol: '600519', market: 'A股' }).value).toBe(242)
    expect(inferTdpy({ symbol: '300750' }).basis).toBe('cn')
  })

  it('未知：null / 异常输入 → 365 / fallback', () => {
    expect(inferTdpy(null)).toEqual({ value: 365, basis: 'fallback', label: '默认 365' })
    expect(inferTdpy({}).basis).toBe('fallback')
    expect(inferTdpy({ symbol: '???' }).basis).toBe('fallback')
  })
})
```

- [ ] **Step 1.2：运行测试确认 FAIL**

```bash
pnpm exec vitest run src/domain/__tests__/tdpy.test.js
```

Expected: 失败，`Cannot find module '../market/tdpy.js'`。

- [ ] **Step 1.3：实现 `tdpy.js`**

`src/domain/market/tdpy.js`：

```js
/**
 * 品种 → 年交易日数（tdpy）自动判断
 *
 * 设计：返回 { value, basis, label }，basis 用于 UI 显示来源
 * 判定顺序：加密 → 美股 → 港股 → A 股 → fallback
 */

const TDPY = {
  crypto: { value: 365, basis: 'crypto', label: '加密 365' },
  us:     { value: 252, basis: 'us',     label: '美股 252' },
  hk:     { value: 242, basis: 'hk',     label: '港股 242' },
  cn:     { value: 242, basis: 'cn',     label: 'A 股 242' },
  fallback: { value: 365, basis: 'fallback', label: '默认 365' },
}

const CRYPTO_TOKENS = ['BTC', 'ETH', 'USDT', 'USDC', 'BNB', 'SOL']

export function inferTdpy(sample) {
  if (!sample || typeof sample !== 'object') return { ...TDPY.fallback }
  const market = sample.market
  const symbol = String(sample.symbol || '').toUpperCase()

  // 1. 加密
  if (market === '加密') return { ...TDPY.crypto }
  if (CRYPTO_TOKENS.some((t) => symbol.includes(t))) return { ...TDPY.crypto }

  // 2. 港股优先于"全字母"判定（避免 .HK 走美股分支）
  if (market === '港股') return { ...TDPY.hk }
  if (/\.HK$/.test(symbol)) return { ...TDPY.hk }

  // 3. A 股
  if (market === 'A股') return { ...TDPY.cn }
  if (/^\d{6}$/.test(symbol)) return { ...TDPY.cn }

  // 4. 美股：纯字母 symbol
  if (market === '美股') return { ...TDPY.us }
  if (/^[A-Z]{1,5}$/.test(symbol)) return { ...TDPY.us }

  return { ...TDPY.fallback }
}
```

- [ ] **Step 1.4：运行测试确认 PASS**

```bash
pnpm exec vitest run src/domain/__tests__/tdpy.test.js
```

Expected: 6 passed。

- [ ] **Step 1.5：跑一次全量测试 + verify**

```bash
pnpm run test && pnpm run verify:domain
```

Expected: 全过。

---

### Task 2：`usePlanning` 加 `tdpyOverride`

**Files:**
- Modify: `src/composables/usePlanning.js`

- [ ] **Step 2.1：删 input 中的 tradingDaysPerYear，新增 tdpyOverride**

`src/composables/usePlanning.js` 第 18-41 行的 `persistedReactive` 调用：

```js
const input = persistedReactive('lab.input.v1', {
  entryPrice: 0,
  holdingDays: 30,
  iv: 0,
  targetReturn: 0.3,
  capital: 10000,
  baseNotional: 0,
  autoProfile: true,
  strategyProfile: 'balanced',
  strikePrice: 0,
  riskFreeRate: 0.04,
  optionType: 'put',
  startPrice: 0,
  rangeWidth: 0.1,
  skew: 1,
  liquidity: 1,
  hedgeSize: 0,
  fees: 0,
  perpTwap: 0,
  spotTwap: 0,
  pathUsesScenarioInputs: false,
  replayFeeRate: 0.001,
  // tradingDaysPerYear 已迁移到 effectiveTdpy（labStore 计算），UI 不再直接读
})
```

直接删除 `tradingDaysPerYear: 365,` 这一行。

- [ ] **Step 2.2：新增 tdpyOverride 持久化对象**

紧跟在 `const input = persistedReactive(...)` 之后插入：

```js
const tdpyOverride = persistedReactive('lab.tdpyOverride.v1', {})

function setTdpyOverride(symbol, value) {
  if (!symbol) return
  if (value === null || !Number.isFinite(value) || value <= 0) {
    delete tdpyOverride[symbol]
    return
  }
  tdpyOverride[symbol] = value
}

function clearTdpyOverride(symbol) {
  if (symbol) delete tdpyOverride[symbol]
}
```

> 注：`persistedReactive` 用 `reactive(...)` 包了对象，可以直接 `delete tdpyOverride[symbol]`，watcher 触发持久化。

- [ ] **Step 2.3：导出 tdpyOverride / setter / clearer**

`return { ... }` 中追加：

```js
return {
  input,
  tdpyOverride,
  setTdpyOverride,
  clearTdpyOverride,
  activeMode,
  activeCapabilityId,
  activeFormulaId,
  activeCapability,
  activeCapabilityStages,
  activeFormula,
  formulaCapabilities,
  strategyProfileList,
  selectCapability,
}
```

- [ ] **Step 2.4：跑测试确认未引入回归**

```bash
pnpm run test
```

Expected: 全过（usePlanning 没有专门测试，所以这里只是确认其他测试不受连带影响）。

---

### Task 3：`useMarketState` 改吃 `effectiveInput`

**Files:**
- Modify: `src/composables/useMarketState.js`

> 设计变更：`useMarketState(rows, cursor, input)` 改为 `useMarketState(rows, cursor, effectiveInput)`，内部直接读 `effectiveInput.tradingDaysPerYear`。`buildFormulaPath` 现在接收 `input`，需确认它仍读 `input.tradingDaysPerYear`（labStore 那边会传 `effectiveInput`）。

- [ ] **Step 3.1：改函数签名注释**

`src/composables/useMarketState.js` 整体重写：

```js
import { computed } from 'vue'
import { buildCostPath, buildMarketStatePath } from '../domain/market/cost.js'
import { buildFormulaPath } from '../domain/market/formulaPath.js'

/**
 * 市场态计算层：滚动 VWAP 成本带、年化波动、ATR、动量、Δ 价格带等
 *
 * 二级缓存：rows → tdpy → marketStates
 *
 * @param {Ref<Array>} rows
 * @param {Ref<number>} cursor
 * @param {ComputedRef<object>} effectiveInput  必须含 tradingDaysPerYear（来自 labStore.effectiveInput）
 */
export function useMarketState(rows, cursor, effectiveInput) {
  const marketStatePathCache = new WeakMap()

  function getMarketStatePath(r) {
    const tdpy = Number(effectiveInput.value.tradingDaysPerYear) || 365
    let bucket = marketStatePathCache.get(r)
    if (!bucket) {
      bucket = new Map()
      marketStatePathCache.set(r, bucket)
    }
    if (!bucket.has(tdpy)) bucket.set(tdpy, buildMarketStatePath(r, tdpy))
    return bucket.get(tdpy)
  }

  const activeRows = computed(() => rows.value.slice(0, cursor.value + 1))
  const marketStateFull = computed(() => rows.value.length ? getMarketStatePath(rows.value) : [])
  const market = computed(() => marketStateFull.value[cursor.value] ?? null)
  const costPath = computed(() => buildCostPath(rows.value))
  const formulaPath = computed(() => buildFormulaPath(rows.value, effectiveInput.value))

  return { activeRows, marketStateFull, market, costPath, formulaPath }
}
```

- [ ] **Step 3.2：暂不跑测试**（labStore 还没改，编译不通过是预期的，等 Task 4 一起跑）

---

### Task 4：`labStore` 注入 `effectiveTdpy` 与新 API

**Files:**
- Modify: `src/stores/labStore.js`

- [ ] **Step 4.1：import `inferTdpy`**

第 1-9 行 import 段，追加：

```js
import { inferTdpy } from '../domain/market/tdpy.js'
```

- [ ] **Step 4.2：重写 store 主体的 effectiveInput / 下游接线**

替换第 24-72 行：

```js
  // 1. 输入与 UI 状态
  const planning = usePlanning()
  const { input } = planning

  // 2. 数据加载
  const data = useDataLoader(input)
  const { rows, cursor } = data

  // 3. tdpy：按品种自动 + 用户覆盖
  const tdpyMeta = computed(() => inferTdpy(data.source.value))
  const effectiveTdpy = computed(() => {
    const sym = data.source.value?.symbol
    const override = sym ? planning.tdpyOverride[sym] : null
    return Number.isFinite(override) && override > 0 ? override : tdpyMeta.value.value
  })

  // 4. 占位 effectiveInput（initialize-after-use 模式：先声明便于下游引用）
  //    profile 字段在 replayLayer 初始化后再注入
  const effectiveInput = computed(() => ({
    ...input,
    tradingDaysPerYear: effectiveTdpy.value,
    strategyProfile: input.autoProfile && replayLayer
      ? replayLayer.recommendedProfile.value.id
      : input.strategyProfile,
  }))

  // 5. 市场态（吃 effectiveInput，确保 tdpy 自动跟随）
  const marketState = useMarketState(rows, cursor, effectiveInput)

  // 6. 回放（复用 marketStateFull）
  let replayLayer = useReplay(rows, input, effectiveInput, marketState.marketStateFull)

  // 7. 决策图
  const graph = computed(() => buildDecisionGraph({
    market: marketState.market.value,
    input: effectiveInput.value,
  }))

  const executionBrief = computed(() => buildExecutionBrief(
    graph.value,
    replayLayer.recommendedProfile.value,
    input.autoProfile,
  ))

  const sourceLabel = computed(() => data.source.value?.label ?? '未载入')

  // 加载新 rows 时回填输入参数（用 effectiveTdpy 而非 input.tradingDaysPerYear）
  watch(rows, (next) => {
    if (!next.length) return
    const tdpy = effectiveTdpy.value
    const lastMarket = buildMarketStatePath(next, tdpy).at(-1)
    if (!lastMarket) return
    input.entryPrice = round(lastMarket.markPrice, 2)
    input.iv = round(lastMarket.annualVol, 4)
    input.strikePrice = round(lastMarket.markPrice * 1.05, 2)
    input.startPrice = round(lastMarket.costAnchor, 2)
    input.perpTwap = round(lastMarket.markPrice, 2)
    input.spotTwap = round(lastMarket.costAnchor, 2)
  })
```

- [ ] **Step 4.3：扩 return 暴露 tdpy 相关字段**

在 return 块（约第 79 行起）补充：

```js
    // tdpy 层（新）
    tdpyMeta,
    effectiveTdpy,
    setTdpyOverride: planning.setTdpyOverride,
    clearTdpyOverride: planning.clearTdpyOverride,
    tdpyOverride: planning.tdpyOverride,
```

放在 `// 数据层` 块之后、`// 市场态层` 块之前更醒目；具体位置不影响行为。

- [ ] **Step 4.4：运行 store 集成测试**

```bash
pnpm exec vitest run src/stores/__tests__/labStore.test.js
```

Expected: 现有测试全过。如果有用例显式读 `input.tradingDaysPerYear`，改成读 `effectiveInput.tradingDaysPerYear`（先看 Step 4.5 全量结果再决定）。

- [ ] **Step 4.5：全量测试 + verify**

```bash
pnpm run test && pnpm run verify:domain
```

Expected: 全过。**如有 fail：检查测试是否假定了旧的 `input.tradingDaysPerYear` 字段；改成读 `effectiveInput`/显式传 tdpy 给被测函数。**

---

### Task 5：labStore 新增"切品种 effectiveTdpy 跟随 / 手动覆盖优先"测试

**Files:**
- Modify: `src/stores/__tests__/labStore.test.js`

- [ ] **Step 5.1：先看测试现状**

```bash
pnpm exec vitest run src/stores/__tests__/labStore.test.js --reporter=verbose
```

记下当前用例数（应是 6 个）。

- [ ] **Step 5.2：追加 2 个新用例**

在 `src/stores/__tests__/labStore.test.js` 现有 describe 块末尾追加：

```js
  it('effectiveTdpy 默认按 source 自动判断', async () => {
    const lab = useLabStore()
    // 模拟切到 BTC（symbol 含 BTC → crypto → 365）
    lab.source.value = { id: 'x', symbol: 'BTCUSDT', label: 'BTC', url: '' }
    expect(lab.tdpyMeta.value.basis).toBe('crypto')
    expect(lab.effectiveTdpy.value).toBe(365)

    lab.source.value = { id: 'y', symbol: 'AAPL', market: '美股', label: 'AAPL', url: '' }
    expect(lab.tdpyMeta.value.basis).toBe('us')
    expect(lab.effectiveTdpy.value).toBe(252)
  })

  it('用户覆盖优先于自动判断，且 per-symbol 隔离', () => {
    const lab = useLabStore()
    lab.source.value = { id: 'a', symbol: 'AAPL', market: '美股', label: 'AAPL', url: '' }
    lab.setTdpyOverride('AAPL', 365)
    expect(lab.effectiveTdpy.value).toBe(365)

    // 切到另一个品种，覆盖不会跨品种污染
    lab.source.value = { id: 'b', symbol: 'BTCUSDT', label: 'BTC', url: '' }
    expect(lab.effectiveTdpy.value).toBe(365)  // 加密本身就是 365
    lab.setTdpyOverride('BTCUSDT', 252)
    expect(lab.effectiveTdpy.value).toBe(252)

    // 回到 AAPL，仍是之前的覆盖值 365
    lab.source.value = { id: 'a', symbol: 'AAPL', market: '美股', label: 'AAPL', url: '' }
    expect(lab.effectiveTdpy.value).toBe(365)

    // 清除 AAPL 覆盖，回到自动 252
    lab.clearTdpyOverride('AAPL')
    expect(lab.effectiveTdpy.value).toBe(252)
  })
```

> 提示：现有测试如何拿 `lab.source` / `lab.tdpyMeta` 取决于 store 暴露方式。如果 `data.source` 没暴露 `.value`（被解包了），改成 `lab.source = {...}`。先 cat 测试文件检查写法再贴。

- [ ] **Step 5.3：运行新增测试确认 PASS**

```bash
pnpm exec vitest run src/stores/__tests__/labStore.test.js
```

Expected: 8 passed（原 6 + 新 2）。

- [ ] **Step 5.4：全量回归**

```bash
pnpm run test && pnpm run verify:domain && pnpm run check:data
```

Expected: 全过。

---

### Task 6：新增 `AdvancedSettings.vue`

**Files:**
- Create: `src/components/AdvancedSettings.vue`

- [ ] **Step 6.1：创建组件**

`src/components/AdvancedSettings.vue`：

```vue
<script setup>
import { computed, ref } from 'vue'

const props = defineProps({
  tdpyMeta: { type: Object, required: true },           // { value, basis, label }
  effectiveTdpy: { type: Number, required: true },
  symbol: { type: String, default: '' },
  open: { type: Boolean, default: false },
})

const emit = defineEmits(['override', 'reset', 'toggle'])

const PRESETS = [
  { value: 365, label: '365' },
  { value: 252, label: '252' },
  { value: 242, label: '242' },
  { value: 179, label: '179' },
]

const customInput = ref('')

const isOverridden = computed(() => props.effectiveTdpy !== props.tdpyMeta.value)

function applyPreset(value) {
  if (!props.symbol) return
  emit('override', props.symbol, value)
}

function applyCustom() {
  const num = Number(customInput.value)
  if (!Number.isFinite(num) || num <= 0) {
    customInput.value = ''
    return
  }
  // clamp 到 60..400 防止离谱值
  const clamped = Math.max(60, Math.min(400, Math.round(num)))
  emit('override', props.symbol, clamped)
  customInput.value = String(clamped)
}

function resetToAuto() {
  emit('reset', props.symbol)
}

function onSummaryClick(e) {
  e.preventDefault()
  emit('toggle', !props.open)
}
</script>

<template>
  <details class="adv-card" :open="open">
    <summary @click="onSummaryClick">
      <span class="adv-title">高级 · 计算口径</span>
      <span class="adv-current">
        年时间基 <strong>{{ effectiveTdpy }}</strong>
        <em :class="['adv-source', { overridden: isOverridden }]">
          {{ isOverridden ? '已手动覆盖' : `按品种自动 · ${tdpyMeta.label}` }}
        </em>
      </span>
    </summary>
    <div class="adv-body">
      <div class="adv-presets">
        <button
          v-for="p in PRESETS"
          :key="p.value"
          type="button"
          :class="['adv-preset', { active: effectiveTdpy === p.value }]"
          :disabled="!symbol"
          @click="applyPreset(p.value)"
        >{{ p.label }}</button>
        <label class="adv-custom">
          <span>自定义</span>
          <input v-model="customInput" type="number" min="60" max="400" placeholder="60-400" @keydown.enter="applyCustom" />
          <button type="button" :disabled="!symbol || !customInput" @click="applyCustom">应用</button>
        </label>
      </div>
      <button type="button" class="adv-reset" :disabled="!isOverridden" @click="resetToAuto">恢复自动</button>
      <p class="adv-hint">
        年时间基用于把日波动年化（IV、Δ 带、BS、回放共用）。日常无需调整；当数据集与默认市场口径不符时再手动覆盖。
      </p>
    </div>
  </details>
</template>

<style>
.adv-card { border: 1px dashed var(--line); border-radius: 7px; background: var(--panel); padding: 6px 8px; }
.adv-card[open] { border-style: solid; }
.adv-card summary { display: flex; justify-content: space-between; align-items: center; gap: 8px; cursor: pointer; list-style: none; }
.adv-card summary::-webkit-details-marker { display: none; }
.adv-title { color: var(--green); font-size: 0.62rem; font-weight: 900; letter-spacing: 0.05em; text-transform: uppercase; }
.adv-current { font-size: 0.74rem; color: var(--ink); display: flex; gap: 6px; align-items: baseline; }
.adv-current strong { font-variant-numeric: tabular-nums; }
.adv-source { font-style: normal; padding: 1px 6px; border-radius: 999px; border: 1px solid var(--line); color: var(--muted); font-size: 0.62rem; }
.adv-source.overridden { border-color: #b8860b; color: #b8860b; }
.adv-body { display: grid; gap: 6px; padding: 6px 0 2px; }
.adv-presets { display: flex; gap: 4px; flex-wrap: wrap; align-items: center; }
.adv-preset { min-height: 26px; padding: 2px 9px; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.74rem; font-weight: 700; cursor: pointer; }
.adv-preset.active { border-color: var(--green); background: var(--surface-active); }
.adv-preset:disabled { opacity: 0.4; cursor: not-allowed; }
.adv-custom { display: inline-flex; gap: 3px; align-items: center; font-size: 0.66rem; color: var(--muted); }
.adv-custom input { width: 70px; min-height: 26px; border: 1px solid var(--line); border-radius: 4px; padding: 2px 5px; background: var(--bg); color: var(--ink); font-variant-numeric: tabular-nums; }
.adv-custom button { min-height: 26px; padding: 1px 7px; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.7rem; cursor: pointer; }
.adv-reset { width: fit-content; min-height: 26px; padding: 1px 9px; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.7rem; cursor: pointer; }
.adv-reset:disabled { opacity: 0.4; cursor: not-allowed; }
.adv-hint { margin: 0; padding: 4px 0 0; color: var(--muted); font-size: 0.62rem; line-height: 1.4; border-top: 1px dashed var(--line); }
</style>
```

> 注：`<details>` 默认行为是点击 summary 切换 open；这里我们通过 `e.preventDefault()` + emit('toggle') 让父组件持久化展开态。

---

### Task 7：`App.vue` 接 AdvancedSettings + 删 tdpy 下拉

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 7.1：import 新组件 + 持久化展开态**

`<script setup>` 顶部 import 段加：

```js
import AdvancedSettings from './components/AdvancedSettings.vue'
```

紧跟在 `const drawerOpen = ref(false)` 之后插入：

```js
const advancedOpen = persistedRef('lab.advancedOpen.v1', false)
function toggleAdvanced(next) { advancedOpen.value = next }
```

- [ ] **Step 7.2：删公式参数区的 tdpy `<select>`**

定位 `<div class="wf-params">` 中的：

```vue
<label><span>年时间基</span><select v-model.number="lab.input.tradingDaysPerYear" class="ws-sel"><option :value="365">365 日历</option><option :value="252">252 美股</option><option :value="242">242 港股</option><option :value="179">179 质数基</option></select></label>
```

整行删除。

- [ ] **Step 7.3：在 wb-right 末尾挂 AdvancedSettings**

在 `<aside class="wb-right">` 内、`<OrderTable>` / `risk-box` 块之后追加：

```vue
        <AdvancedSettings
          :tdpy-meta="lab.tdpyMeta"
          :effective-tdpy="lab.effectiveTdpy"
          :symbol="lab.source?.symbol ?? ''"
          :open="advancedOpen"
          @override="(sym, val) => lab.setTdpyOverride(sym, val)"
          @reset="(sym) => lab.clearTdpyOverride(sym)"
          @toggle="toggleAdvanced"
        />
```

- [ ] **Step 7.4：检查 build**

```bash
pnpm run build
```

Expected: build 成功（含 `check:data` + `verify:domain` + `vite build`）。

- [ ] **Step 7.5：手动验证（启动 dev）**

```bash
pnpm run dev
```

打开浏览器，依次：

1. 加载 BTC → 高级设置卡显示"加密 365"，effectiveTdpy = 365 ✓
2. 切到 AAPL → 显示"美股 252"，IV 数值变化 ✓
3. 高级卡点 365 → 显示"已手动覆盖"，刷新后仍是 365 ✓
4. 高级卡点"恢复自动" → 回到 252 ✓
5. 切到 0700.HK → 自动 242 ✓
6. 公式参数区已无 tdpy 下拉 ✓

如全部通过，**PR-1 结束**。

- [ ] **Step 7.6：commit（如果用了 git）**

```bash
git add src/domain/market/tdpy.js src/domain/__tests__/tdpy.test.js src/composables/usePlanning.js src/composables/useMarketState.js src/stores/labStore.js src/stores/__tests__/labStore.test.js src/components/AdvancedSettings.vue src/App.vue docs/superpowers/specs/2026-05-14-lab-v2-design-cc.md docs/superpowers/plans/2026-05-14-lab-v2-cc.md
git commit -m "feat(lab): 日基弱化 — 按品种自动 tdpy + 高级覆盖卡"
```

---

## PR-2：图表解释

### Task 8：`domain/decision/narrative.js` 与单测

**Files:**
- Create: `src/domain/decision/narrative.js`
- Create: `src/domain/__tests__/narrative.test.js`

- [ ] **Step 8.1：写失败测试**

`src/domain/__tests__/narrative.test.js`：

```js
import { describe, expect, it } from 'vitest'
import { summarizeRegime, summarizeRegression, summarizeReason } from '../decision/narrative.js'

describe('summarizeRegime', () => {
  it('深度折价：低于 5%', () => {
    const out = summarizeRegime({ markPrice: 90, costAnchor: 100, costDistance: -0.10, costWindow: 60 })
    expect(out).toContain('深度折价')
    expect(out).toContain('10.0%')
    expect(out).toContain('近 60 日均价')
  })

  it('小幅折价：1%~5%', () => {
    const out = summarizeRegime({ markPrice: 97, costAnchor: 100, costDistance: -0.03, costWindow: 60 })
    expect(out).toContain('小幅折价')
    expect(out).toContain('3.0%')
  })

  it('贴近均价：abs ≤ 1%', () => {
    const out = summarizeRegime({ markPrice: 100.5, costAnchor: 100, costDistance: 0.005, costWindow: 60 })
    expect(out).toContain('贴近均价')
  })

  it('溢价：高于 5%', () => {
    const out = summarizeRegime({ markPrice: 110, costAnchor: 100, costDistance: 0.10, costWindow: 60 })
    expect(out).toContain('溢价')
    expect(out).toContain('10.0%')
  })

  it('回归区：1%~5%', () => {
    const out = summarizeRegime({ markPrice: 103, costAnchor: 100, costDistance: 0.03, costWindow: 60 })
    expect(out).toContain('回归')
  })

  it('null 容错', () => {
    expect(summarizeRegime(null)).toBe('载入 K 线后判断')
    expect(summarizeRegime({})).toBe('载入 K 线后判断')
  })
})

describe('summarizeRegression', () => {
  it('正常输出概率短句', () => {
    const out = summarizeRegression({ regressionProb: 0.65, regime: '折价' })
    expect(out).toContain('65%')
    expect(out).toContain('回归')
  })

  it('null/无概率：返回 null', () => {
    expect(summarizeRegression(null)).toBeNull()
    expect(summarizeRegression({ regressionProb: null })).toBeNull()
  })
})

describe('summarizeReason', () => {
  it('折价 + side=buy → "再等更便宜或开始买"风格', () => {
    const out = summarizeReason({ state: '折价买入', costDistance: -0.03, atrPercent: 0.02, side: 'buy' })
    expect(typeof out).toBe('string')
    expect(out.length).toBeGreaterThan(4)
  })

  it('溢价 + side=sell → "仅减仓"风格', () => {
    const out = summarizeReason({ state: '溢价减仓', costDistance: 0.06, atrPercent: 0.02, side: 'sell' })
    expect(out).toContain('仓')
  })

  it('null 输入兜底', () => {
    expect(summarizeReason(null)).toBe('等待 K 线给出明确折价')
    expect(summarizeReason({})).toBe('等待 K 线给出明确折价')
  })
})
```

- [ ] **Step 8.2：运行确认 FAIL**

```bash
pnpm exec vitest run src/domain/__tests__/narrative.test.js
```

Expected: 失败，模块未找到。

- [ ] **Step 8.3：实现 `narrative.js`**

`src/domain/decision/narrative.js`：

```js
/**
 * 把决策图字段拼装成人话短句
 *
 * 设计原则：
 *   - 纯函数，输入是已经算好的数值，不重算
 *   - 输入异常 / null 给安全兜底字符串
 *   - 文案风格：陈述句，不卖弄术语；保留具体数字（X%）增强可信
 */

function pct(value, digits = 1) {
  if (!Number.isFinite(value)) return ''
  return `${(Math.abs(value) * 100).toFixed(digits)}%`
}

/**
 * 区间摘要：现价相对成本带的位置
 * @returns {string}
 */
export function summarizeRegime(payload) {
  if (!payload || typeof payload !== 'object') return '载入 K 线后判断'
  const { costDistance, costWindow } = payload
  if (!Number.isFinite(costDistance)) return '载入 K 线后判断'
  const window = Number.isFinite(costWindow) && costWindow > 0 ? Math.round(costWindow) : 60
  const baseline = `近 ${window} 日均价`
  const p = pct(costDistance, 1)

  if (costDistance < -0.05) return `深度折价 ${p}，${baseline}下方较远，适合分批吸纳`
  if (costDistance < -0.01) return `小幅折价 ${p}，低于${baseline}，可再等更便宜或开始买`
  if (costDistance > 0.05)  return `溢价 ${p}，高于${baseline}较多，仅做仓位保护`
  if (costDistance > 0.01)  return `回归区上沿 ${p}，略高于${baseline}，观察方向`
  return `贴近${baseline}，等价格给出明确折价`
}

/**
 * 回归概率短句
 * @returns {string|null}
 */
export function summarizeRegression(payload) {
  if (!payload || typeof payload !== 'object') return null
  const { regressionProb } = payload
  if (!Number.isFinite(regressionProb)) return null
  const p = Math.round(regressionProb * 100)
  return `历史上类似偏离，${p}% 概率回归均价`
}

/**
 * 决策原因人话化
 * @returns {string}
 */
export function summarizeReason(payload) {
  if (!payload || typeof payload !== 'object') return '等待 K 线给出明确折价'
  const { costDistance, side } = payload
  if (!Number.isFinite(costDistance)) return '等待 K 线给出明确折价'
  const p = pct(costDistance, 1)

  if (side === 'sell') {
    if (costDistance > 0.05) return `溢价 ${p}，已偏离均价较多，只用底仓减压`
    return `溢价 ${p}，仅做仓位保护，不主动加仓`
  }
  if (side === 'buy') {
    if (costDistance < -0.05) return `折价 ${p}，价格便宜了不少，可分批吸筹`
    if (costDistance < -0.01) return `小幅折价 ${p}，再等更便宜一点或轻仓试探`
    return '回到均价附近，等下一次明确折价'
  }
  if (Math.abs(costDistance) < 0.01) return '价格贴近均价，再等明确方向'
  return costDistance < 0
    ? `折价 ${p}，等动量止跌信号`
    : `溢价 ${p}，仅观察不进场`
}
```

- [ ] **Step 8.4：运行确认 PASS**

```bash
pnpm exec vitest run src/domain/__tests__/narrative.test.js
```

Expected: 11 passed（6+2+3）。

---

### Task 9：`MarketChart` 接入摘要句

**Files:**
- Modify: `src/components/MarketChart.vue`
- Modify: `src/App.vue`（仅 props 透传）

- [ ] **Step 9.1：MarketChart 加 props.market + import narrative + 拼摘要**

`src/components/MarketChart.vue` 顶部 `<script setup>`：

import 段追加：

```js
import { summarizeRegime, summarizeRegression } from '../domain/decision/narrative.js'
import { deriveWindows } from '../domain/market/cost.js'
import { deviationScore } from '../domain/formulas/core.js'
```

`defineProps` 加 `market`：

```js
const props = defineProps({
  rows: { type: Array, required: true },
  costPath: { type: Array, required: true },
  formulaPath: { type: Array, required: true },
  entryPrice: { type: Number, required: true },
  replay: { type: Object, required: true },
  market: { type: Object, default: null },
})
```

紧跟在 `const regimeCounts = computed(...)` 之后追加：

```js
const narrative = computed(() => {
  const m = props.market
  if (!m) return { regimeLine: '载入 K 线后判断', probLine: null }
  const windowSize = props.rows.length ? deriveWindows(props.rows.length).cost : 60
  const regimeLine = summarizeRegime({
    markPrice: m.markPrice,
    costAnchor: m.costAnchor,
    costDistance: m.costDistance,
    costWindow: windowSize,
  })
  // 给 deviationScore 一个保守的 holdingDays/tdpy（仅用于回归概率显示）
  const ds = deviationScore({
    costDistance: m.costDistance,
    annualVol: m.annualVol,
    holdingDays: 30,
    tradingDaysPerYear: 365,
  })
  const probLine = ds ? summarizeRegression(ds) : null
  return { regimeLine, probLine }
})
```

> 注：`deviationScore` 的 holdingDays/tdpy 这里只为生成"概率短句"做近似展示，与实际策略 holdingDays 解耦，避免 prop 链路过深。spec §3.4 已批准用近似值。

- [ ] **Step 9.2：改造 chart-state-strip 第 1 个 article**

定位 `<article :class="['state-chip', activeRegime.tone]">` 块，整体替换为：

```vue
<article class="state-chip narrative-chip">
  <span>当前判断</span>
  <strong class="narr-text">{{ narrative.regimeLine }}</strong>
  <small class="narr-prob">{{ narrative.probLine || activeRegime.detail }}</small>
</article>
```

- [ ] **Step 9.3：补 `.narrative-chip` 样式**

`<style>` 末尾追加：

```css
.narrative-chip { gap: 3px; }
.narrative-chip .narr-text { font-size: 0.86rem; line-height: 1.3; font-weight: 700; }
.narrative-chip .narr-prob { color: var(--muted); font-size: 0.66rem; line-height: 1.3; }
```

> 现有 `.state-chip` 类已有基础 padding/border，narrative-chip 只覆盖文本节奏。

- [ ] **Step 9.4：App.vue 给 MarketChart 传 market**

`src/App.vue` 模板里：

```vue
<MarketChart :rows="lab.rows" :cost-path="lab.costPath" :formula-path="lab.formulaPath" :entry-price="lab.input.entryPrice" :replay="lab.replay" @cursor-change="(idx) => { if (idx !== null) lab.cursor = idx }" />
```

改为：

```vue
<MarketChart :rows="lab.rows" :cost-path="lab.costPath" :formula-path="lab.formulaPath" :entry-price="lab.input.entryPrice" :replay="lab.replay" :market="lab.market" @cursor-change="(idx) => { if (idx !== null) lab.cursor = idx }" />
```

- [ ] **Step 9.5：跑测试 + 启动 dev 验证**

```bash
pnpm run test && pnpm run dev
```

打开浏览器加载任一品种 → 主图上方左侧 chip 显示一行人话摘要 + 一行概率短句 ✓

---

### Task 10：`DecisionPanel` reason 走 narrative

**Files:**
- Modify: `src/components/DecisionPanel.vue`

- [ ] **Step 10.1：import + 拼接**

`<script setup>` 头部加：

```js
import { computed } from 'vue'
import { summarizeReason } from '../domain/decision/narrative.js'
```

`<script setup>` 内部加：

```js
const reasonText = computed(() => {
  const decision = props.graph?.decision
  const market = props.market
  if (!decision || !market) return decision?.path || '载入 K 线后自动分析'
  const human = summarizeReason({
    state: decision.state,
    costDistance: market.costDistance,
    atrPercent: market.atrPercent,
    side: decision.timing?.side,
  })
  // 保留技术 reason 作为 hover/title 备用
  return human
})
```

> 注：`defineProps` 用了对象语法但没赋给变量。改成 `const props = defineProps({...})` 再用 `props.graph` / `props.market`。

将原来的：

```js
defineProps({
  graph: { type: Object, required: true },
  market: { type: Object, default: null },
})
```

改为：

```js
const props = defineProps({
  graph: { type: Object, required: true },
  market: { type: Object, default: null },
})
```

- [ ] **Step 10.2：模板里替换 decision-hero 的 `<small>`**

```vue
<small :title="graph.decision?.timing?.reason">{{ reasonText }}</small>
```

`title` 给鼠标悬停时显示原始技术 reason，方便调试。

- [ ] **Step 10.3：跑测试**

```bash
pnpm run test
```

Expected: 全过。

- [ ] **Step 10.4：手动验证**

```bash
pnpm run dev
```

加载任一品种，右栏决策卡上方 hero 文案应是人话风格（如"小幅折价 3.0%，再等更便宜一点或轻仓试探"），hover 还能看到原 reason。

---

### Task 11：PR-2 收尾

- [ ] **Step 11.1：全量回归**

```bash
pnpm run check:data && pnpm run verify:domain && pnpm run test && pnpm run build
```

Expected: 全过。

- [ ] **Step 11.2：commit**

```bash
git add src/domain/decision/narrative.js src/domain/__tests__/narrative.test.js src/components/MarketChart.vue src/components/DecisionPanel.vue src/App.vue
git commit -m "feat(lab): 图表与决策面板接入 narrative 人话摘要"
```

---

## PR-3：交互骨架

### Task 12：QuestionNav 砍 Q3

**Files:**
- Modify: `src/components/QuestionNav.vue`

- [ ] **Step 12.1：删除 q-edge 块**

定位 `questions` 数组中 id 为 `'q-edge'` 的块（第 36-44 行）：

```js
{
  id: 'q-edge',
  title: '3 · 盈利来源是哪一类？',
  hint: '四选一：成本回归 · 手续费 · 波动率 · 资金费率',
  options: [
    { id: 'edge-cost', label: '成本回归', formula: 'deviation-score' },
    { id: 'edge-fees', label: '手续费收益', formula: 'net-lp-efficiency' },
    { id: 'edge-vol',  label: '波动率错配', formula: 'risk-surface' },
    { id: 'edge-fund', label: '资金费率', formula: 'net-carry' },
  ],
},
```

整段删除。

- [ ] **Step 12.2：调整剩余问题序号**

把 `'2 · 入场价与持仓窗口？'` 改为 `'2 · 入场价与持仓窗口？'`（不变）；
把 `'4 · 这条路径上哪里失效？'` 改为 `'3 · 这条路径上哪里失效？'`（重新编号）。

- [ ] **Step 12.3：删除 selectEdgeOption 函数 + 模板里的 qn-opts 区**

`<script setup>`：

```js
function selectEdgeOption(opt) {
  emit('select-formula', opt.formula)
}
```

整段删。

模板里：

```vue
<div v-if="q.options" class="qn-opts">
  <button
    v-for="opt in q.options"
    :key="opt.id"
    type="button"
    class="qn-opt"
    :class="{ active: opt.formula === activeFormulaId }"
    @click="selectEdgeOption(opt)"
  >
    {{ opt.label }}
  </button>
</div>
```

整段删。

- [ ] **Step 12.4：更新 qn-hint 文案**

```vue
<p class="qn-hint">公式只在点"为什么"时出现；想看完整公式列表请展开下方"完整公式列表"。</p>
```

- [ ] **Step 12.5：删 .qn-opts / .qn-opt 样式**

`<style>` 块中：

```css
.qn-opts { display: grid; grid-template-columns: 1fr 1fr; gap: 4px; padding: 0 4px; }
.qn-opt { min-height: 28px; border: 1px solid var(--line); border-radius: 5px; background: transparent; color: var(--ink); font-size: 0.7rem; font-weight: 700; cursor: pointer; }
.qn-opt:hover { border-color: var(--green); }
.qn-opt.active { border-color: var(--green); background: var(--surface-active); }
```

整段删。

- [ ] **Step 12.6：跑测试 + 启动验证**

```bash
pnpm run test && pnpm run dev
```

左栏只有 3 个问题块；折叠的"完整公式列表"仍可用。

---

### Task 13：ChainFlow 套 details 折叠

**Files:**
- Modify: `src/components/ChainFlow.vue`

- [ ] **Step 13.1：模板套 `<details>`**

把当前 `<template>` 中的 `<div class="cf-strip">` 整段包一层：

```vue
<template>
  <details class="cf-toggle">
    <summary>查看计算过程（基础管线 → 公式融合）</summary>
    <div class="cf-strip">
      <!-- 原内容不动 -->
      <div class="cf-layer">…</div>
      <div class="cf-layer">…</div>
    </div>
  </details>
</template>
```

> 把现有 `<div class="cf-strip">…</div>` 整体放进 `<details>` 内即可，**内部内容一行不动**。

- [ ] **Step 13.2：补样式**

`<style>` 末尾追加：

```css
.cf-toggle { border-bottom: 1px solid var(--line); background: var(--panel); }
.cf-toggle > summary { cursor: pointer; padding: 5px 10px; color: var(--muted); font-size: 0.66rem; font-weight: 800; letter-spacing: 0.04em; text-transform: uppercase; list-style: none; }
.cf-toggle > summary::-webkit-details-marker { display: none; }
.cf-toggle > summary::before { content: '▶ '; display: inline-block; margin-right: 4px; transition: transform 0.15s; }
.cf-toggle[open] > summary::before { transform: rotate(90deg); }
.cf-toggle > summary:hover { color: var(--ink); }
.cf-toggle > .cf-strip { border-bottom: none; }  /* 内部已有原样式，避免重复底边 */
```

- [ ] **Step 13.3：验证**

```bash
pnpm run dev
```

K 线下方 ChainFlow 默认收起，点击展开后内容如旧。

---

### Task 14：新增 `ProfileTabs.vue`

**Files:**
- Create: `src/components/ProfileTabs.vue`

- [ ] **Step 14.1：建组件**

`src/components/ProfileTabs.vue`：

```vue
<script setup>
defineProps({
  autoProfile: { type: Boolean, required: true },
  activeId: { type: String, required: true },
  profileList: { type: Array, required: true },
})

defineEmits(['set-auto', 'select'])

const SUBTITLES = {
  conservative: '成本回归',
  balanced:     '成本 + 波动',
  aggressive:   '波动 + 资金',
}
</script>

<template>
  <div class="profile-tabs">
    <button
      type="button"
      :class="['pt-btn', { active: autoProfile }]"
      @click="$emit('set-auto')"
    >
      <strong>接管</strong>
      <small>手动控制</small>
    </button>
    <button
      v-for="p in profileList"
      :key="p.id"
      type="button"
      :class="['pt-btn', { active: !autoProfile && activeId === p.id }]"
      @click="$emit('select', p.id)"
    >
      <strong>{{ p.label }}</strong>
      <small>{{ SUBTITLES[p.id] || '—' }}</small>
    </button>
  </div>
</template>

<style>
.profile-tabs { display: grid; grid-template-columns: repeat(4, 1fr); gap: 3px; }
.pt-btn { display: grid; gap: 1px; padding: 5px 6px; min-height: 38px; border: 1px solid var(--line); border-radius: 5px; background: var(--bg); color: var(--ink); cursor: pointer; }
.pt-btn strong { font-size: 0.74rem; font-weight: 800; line-height: 1.1; }
.pt-btn small { font-size: 0.6rem; color: var(--muted); line-height: 1.2; }
.pt-btn.active { border-color: var(--green); background: var(--surface-active); }
.pt-btn.active small { color: var(--ink); }
.pt-btn:hover { border-color: var(--green); }
</style>
```

> "接管"按钮的语义：用户从 auto 选档切到手动；点击 profile 按钮切回手动 + 选中那档。

---

### Task 15：新增 `RightTabs.vue`

**Files:**
- Create: `src/components/RightTabs.vue`

- [ ] **Step 15.1：建组件**

`src/components/RightTabs.vue`：

```vue
<script setup>
import { computed } from 'vue'
import DecisionPanel from './DecisionPanel.vue'
import OrderTable from './OrderTable.vue'
import ReplayPanel from './ReplayPanel.vue'

const props = defineProps({
  modelValue: { type: String, required: true },  // 'decision'|'orders'|'risk'|'formula'
  graph: { type: Object, required: true },
  market: { type: Object, default: null },
  replay: { type: Object, required: true },
  profileReplays: { type: Array, default: () => [] },
  activeProfileId: { type: String, default: 'balanced' },
  activeFormula: { type: Object, default: null },
})

const emit = defineEmits(['update:modelValue'])

function setMode(mode) { emit('update:modelValue', mode) }

const TABS = [
  { id: 'decision', label: '决策' },
  { id: 'orders',   label: '计划' },
  { id: 'risk',     label: '风险' },
  { id: 'formula',  label: '依据' },
]

const ordersTitle = computed(() =>
  props.graph?.decision?.timing?.side === 'sell' ? '底仓减压' : '分批低价买入'
)

function money(value) {
  if (!Number.isFinite(value)) return '无'
  return new Intl.NumberFormat('zh-CN', { maximumFractionDigits: 2 }).format(value)
}
</script>

<template>
  <div class="right-tabs">
    <nav class="rt-nav">
      <button
        v-for="t in TABS"
        :key="t.id"
        type="button"
        :class="{ active: modelValue === t.id }"
        @click="setMode(t.id)"
      >{{ t.label }}</button>
    </nav>

    <DecisionPanel v-if="modelValue === 'decision'" :graph="graph" :market="market" />

    <OrderTable
      v-else-if="modelValue === 'orders'"
      :title="ordersTitle"
      :orders="graph?.plan?.primaryOrders ?? []"
    />

    <div v-else-if="modelValue === 'risk'" class="rt-risk">
      <div class="risk-box"><span>失效线</span><strong>{{ money(graph?.plan?.invalidation?.lower) }} / {{ money(graph?.plan?.invalidation?.upper) }}</strong></div>
      <div class="risk-box"><span>组合价值</span><strong :class="(graph?.portfolio ?? 0) >= 0 ? 'green' : 'red'">{{ money(graph?.portfolio) }}</strong></div>
      <ReplayPanel :replay="replay" :profile-replays="profileReplays" :active-profile-id="activeProfileId" />
    </div>

    <section v-else-if="modelValue === 'formula'" class="rt-formula">
      <header>{{ activeFormula?.layer || '—' }} · {{ activeFormula?.label || '—' }}</header>
      <p class="rt-role">{{ activeFormula?.role || '当前公式角色' }}</p>
      <div class="rt-io"><b>输入</b><span>{{ activeFormula?.inputs?.join(' / ') || '—' }}</span></div>
      <div class="rt-io"><b>输出</b><span>{{ activeFormula?.outputs?.join(' / ') || '—' }}</span></div>
      <div v-if="activeFormula?.formulas?.length" class="rt-io"><b>公式</b><code v-for="f in activeFormula.formulas" :key="f">{{ f }}</code></div>
    </section>
  </div>
</template>

<style>
.right-tabs { display: grid; gap: 6px; }
.rt-nav { display: grid; grid-template-columns: repeat(4, 1fr); gap: 3px; }
.rt-nav button { min-height: 26px; border: 1px solid var(--line); border-radius: 5px; background: var(--bg); color: var(--ink); font-weight: 800; font-size: 0.72rem; cursor: pointer; }
.rt-nav button.active { border-color: var(--green); background: var(--surface-active); }
.rt-risk { display: grid; gap: 6px; }
.rt-formula { display: grid; gap: 4px; padding: 8px 10px; border: 1px solid var(--line); border-radius: 6px; background: var(--panel); }
.rt-formula header { color: var(--green); font-size: 0.62rem; font-weight: 900; letter-spacing: 0.05em; text-transform: uppercase; }
.rt-formula .rt-role { margin: 0; color: var(--muted); font-size: 0.74rem; line-height: 1.4; }
.rt-formula .rt-io { display: flex; gap: 6px; align-items: baseline; flex-wrap: wrap; }
.rt-formula .rt-io b { color: var(--muted); font-size: 0.66rem; font-weight: 800; text-transform: uppercase; }
.rt-formula .rt-io span { color: var(--ink); font-size: 0.78rem; }
.rt-formula .rt-io code { border: 1px solid var(--line); border-radius: 4px; padding: 1px 5px; background: var(--bg); color: var(--blue); font-size: 0.7rem; }
</style>
```

---

### Task 16：`App.vue` 接入 ProfileTabs + RightTabs + 顶栏精简

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 16.1：import 新组件**

`<script setup>` import 段：

```js
import ProfileTabs from './components/ProfileTabs.vue'
import RightTabs from './components/RightTabs.vue'
```

可同时**移除**已经迁入 RightTabs 的导入：`DecisionPanel`、`OrderTable`、`ReplayPanel`（这些原本在 App.vue 里直接渲染，现在统一通过 RightTabs 渲染，App.vue 不再用）。

- [ ] **Step 16.2：activeMode 默认值改 'decision'**

`usePlanning.js` 第 43 行：

```js
const activeMode = persistedRef('lab.activeMode.v1', 'orders')
```

改为：

```js
const activeMode = persistedRef('lab.activeMode.v1', 'decision')
```

> 旧用户的 storage 仍存 'orders'，会保持原选择不闪；新用户首次进来在"决策"tab。

- [ ] **Step 16.3：顶栏精简（移除品种按钮）**

`<header class="top-bar">` 中：

```vue
<button v-for="s in filteredSamples" :key="s.id" :class="{ on: lab.source?.id === s.id }" :disabled="lab.loadingSampleId === s.id" @click="lab.loadSample(s)">
  <Database :size="14" />{{ lab.loadingSampleId === s.id ? '…' : s.symbol }}
</button>
```

整段删。同时把 datalist 的 option 数据源从 `allSamples` 改成放在搜索框点击时才计算（不动）；可见样本数照旧。

把搜索框宽度增大。`<style>` 中：

```css
.ticker-search { width: 100px; ... }
```

改为：

```css
.ticker-search { width: 180px; ... }
```

并新增逻辑：搜索框 enter 时若有 `filteredSamples` 直接 loadSample（已有 `onSearchEnter`），保留不变。

- [ ] **Step 16.4：右栏改造**

整个 `<aside class="wb-right">` 重写：

```vue
<aside class="wb-right">
  <div class="exec-strip">
    <span>决策 · {{ brief.confidence }}% 置信</span>
    <strong>{{ brief.title }}</strong>
    <em>{{ brief.bias }}</em>
    <div class="exec-nums">
      <div><span>首笔</span><strong>{{ money(brief.notional) }}</strong></div>
      <div><span>挂单</span><strong>{{ money(brief.price) }}</strong></div>
      <div><span>失效</span><strong>{{ money(brief.stop) }}</strong></div>
      <div><span>目标</span><strong>{{ money(brief.target) }}</strong></div>
    </div>
    <small>{{ brief.reason }}</small>
  </div>

  <ProfileTabs
    :auto-profile="lab.input.autoProfile"
    :active-id="lab.input.strategyProfile"
    :profile-list="lab.strategyProfileList"
    @set-auto="lab.input.autoProfile = true"
    @select="(id) => { lab.input.autoProfile = false; lab.input.strategyProfile = id }"
  />

  <RightTabs
    v-model="lab.activeMode"
    :graph="lab.graph"
    :market="lab.market"
    :replay="lab.replay"
    :profile-replays="lab.profileReplays"
    :active-profile-id="lab.effectiveInput.strategyProfile"
    :active-formula="lab.activeFormula"
  />

  <AdvancedSettings
    :tdpy-meta="lab.tdpyMeta"
    :effective-tdpy="lab.effectiveTdpy"
    :symbol="lab.source?.symbol ?? ''"
    :open="advancedOpen"
    @override="(sym, val) => lab.setTdpyOverride(sym, val)"
    @reset="(sym) => lab.clearTdpyOverride(sym)"
    @toggle="toggleAdvanced"
  />
</aside>
```

> `<DecisionPanel>` `<OrderTable>` `<ReplayPanel>` 的内联渲染全部移除（已被 RightTabs 接管）。

- [ ] **Step 16.5：删除遗留的 mode-tabs / prof-tabs 块**

`<aside class="wb-right">` 之前可能存在的：

```vue
<div class="mode-tabs">…</div>
<div class="prof-tabs">…</div>
```

确认已被 Step 16.4 重写覆盖；如还有残留删干净。同时删 `<style>` 里：

```css
.mode-tabs, .prof-tabs { … }
.mode-tabs { … }
.prof-tabs { … }
.mode-tabs button, .prof-tabs button { … }
.mode-tabs button.active, .prof-tabs button.active { … }
```

整组删。

- [ ] **Step 16.6：跑全套**

```bash
pnpm run test && pnpm run build && pnpm run dev
```

手动验证：

1. 顶栏只剩 Logo / 搜索 / 主题 / 重置 ✓
2. 搜索框宽 180px，输入 `AAPL` 回车加载 ✓
3. 右栏顶部：exec-strip → ProfileTabs(4 档+副标) → 4 个 Tab(决策/计划/风险/依据) → AdvancedSettings ✓
4. 切到"风险" tab，看到失效线 + 组合价值 + Replay 面板 ✓
5. 切到"依据" tab，显示当前 activeFormula 的输入/输出/公式 ✓
6. 切到"决策" tab，显示 DecisionPanel ✓

---

### Task 17：清理 App.vue 旧样式

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 17.1：移除已迁出的样式**

`<style>` 中以下样式已不再被任何模板引用（已迁入 RightTabs 或 ProfileTabs），删除：

- `.exec-strip` 块的副本（保留主图相关）
- 上一步删 `.mode-tabs / .prof-tabs` 已完成

> 验证：`grep -E 'class="(mode-tabs|prof-tabs)"' src/App.vue` 应无输出。

- [ ] **Step 17.2：搜索框样式宽度调整**

确认 Step 16.3 已生效。

- [ ] **Step 17.3：跑 build**

```bash
pnpm run build
```

Expected: 全过。

---

### Task 18：PR-3 全量回归

- [ ] **Step 18.1：跑全套 + 手动验证**

```bash
pnpm run check:data && pnpm run verify:domain && pnpm run test && pnpm run build
```

按 spec §10 验收清单逐条手动：

| 操作 | 期望 |
|---|---|
| 加载 BTC | 高级设置卡显示"加密 365" |
| 切到 AAPL | 自动变 252，IV 数值变化 |
| 高级卡点 365 | tdpy 变 365，副标变"已手动覆盖"，重新加载 AAPL 仍是 365 |
| 高级卡点"恢复自动" | 回到 252，覆盖记录清空 |
| 切到 0700.HK | 自动 242 |
| 顶栏数据集按钮 | 已移除 |
| 公式参数区 | 4 个 label，无 tdpy |
| 左栏 | 3 个问题块（无盈利来源 4 选 1） |
| K 线下方 ChainFlow | 默认收起，需点击 details 展开 |
| 图表 chart-state-strip 第 1 格 | 显示人话摘要 + 概率短句 |
| 决策 hero `<small>` | 显示 narrative 短句 |
| 右栏顶部 | exec-strip + ProfileTabs（4 档+副标） |
| 右栏中部 | 4 tabs（决策/计划/风险/依据），切换内容互斥 |
| 风险 tab | 失效/组合 + Replay 面板 |
| 加载 + 刷新页面 | 高级卡覆盖值保留 |

---

### Task 19：PR-3 commit

- [ ] **Step 19.1：commit**

```bash
git add src/components/QuestionNav.vue src/components/ChainFlow.vue src/components/ProfileTabs.vue src/components/RightTabs.vue src/composables/usePlanning.js src/App.vue
git commit -m "feat(lab): 交互骨架 — profile 主轴 + 右栏 4 tab + 顶栏精简"
```

---

## 自审

**1. spec 覆盖检查**：

| spec 节 | 任务 |
|---|---|
| §3.1 tdpy.js | Task 1 |
| §3.2 usePlanning 加 tdpyOverride | Task 2 |
| §3.3 labStore effectiveTdpy | Task 3 + 4 + 5 |
| §3.4 narrative.js | Task 8 |
| §3.5 App.vue 顶栏/参数区/右栏 | Task 7 + 16 + 17 |
| §3.6 QuestionNav 砍 Q3 | Task 12 |
| §3.7 ChainFlow 折叠 | Task 13 |
| §3.8 MarketChart 摘要句 | Task 9 |
| §3.9 DecisionPanel reason | Task 10 |
| §3.10 ProfileTabs | Task 14 |
| §3.11 AdvancedSettings | Task 6 |
| §3.12 RightTabs | Task 15 |

全覆盖。

**2. placeholder 扫描**：所有 step 都有具体代码或具体命令，无 TBD/TODO。verify-domain 在每个 PR 末尾跑。

**3. 类型/方法名一致**：
- `inferTdpy` → 参数 `sample`，返回 `{ value, basis, label }` —— Task 1 定义 / Task 4 / Task 6 使用，一致
- `setTdpyOverride(symbol, value)` / `clearTdpyOverride(symbol)` —— Task 2 定义 / Task 4 转发 / Task 6 emit / Task 7 接 emit，一致
- `summarizeRegime` / `summarizeRegression` / `summarizeReason` —— Task 8 定义 / Task 9 / Task 10 调用，一致
- `tdpyMeta` `effectiveTdpy` —— Task 4 store 暴露 / Task 6 props / Task 7 模板传入，一致

**4. 风险点**：
- Step 5.2 测试是否能直接 set `lab.source.value`：取决于 store return 时是否 unwrap ref。需要在执行 Step 5.2 前先 `cat src/stores/__tests__/labStore.test.js` 看现有测试怎么 set source；如果是 `lab.loadSample(...)` 异步加载，改成那种写法。这条已在 Step 5.2 注释中提示。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-14-lab-v2-cc.md`. Two execution options:

**1. Subagent-Driven（推荐）** — 每个任务派一个 fresh subagent 执行，任务间停下来给你审，迭代快、上下文干净

**2. Inline Execution** — 在当前会话直接执行，按 PR 批次留 checkpoint 给你审

**选哪个？**
