# Market Lab v3.1 · 三栏可折叠面板实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v3 的"主图占满 + FAB 抽屉"改造为三栏（左面板 3 tab 决策/计算/设置 + 中主图 + 右面板 4 市场分组），两侧可折叠到 36px。零 domain 数值回归，复用所有 v3 已完成的 `DecisionDrawer` / `ComputeDrawer` / `SettingsDrawer` / `ChartStatusBar` 内容；删 `ChartFabs.vue` 和 `DrawerHost.vue`。

**Architecture:** App.vue 用 CSS grid（CSS 变量控宽）布三栏；LeftPanel 顶部 3 tab 切换内嵌 `*Drawer` 子组件；RightPanel 用 `inferTdpy(s).basis` 把 `marketSamples + stockIndex` 分成 4 组（加密/美股/港股/A 股），含本地过滤；TopBar 删搜索，剩 Logo / 决策摘要 / ProfileChip / 主题 / 重置；MainChart 删 ChartFabs，靠 ResizeObserver 自动适配 grid 列宽变化。Store 删 `activeDrawer / openDrawer / closeDrawer`，加 `leftPanelOpen / rightPanelOpen / activeLeftTab / toggleLeftPanel / toggleRightPanel`（均 persisted）。

**Tech Stack:** Vue 3 Composition API · Pinia setup store · vitest · `@vue/test-utils` · lightweight-charts v5 · 现有 `usePersisted` localStorage 封装

**Spec:** `docs/superpowers/specs/2026-05-15-lab-v3.1-three-panel-design-cc.md`

**约束**：
- domain 公式数值零回归（`npm run verify:domain` 必须过）
- 复用 v3 已完成的 `DecisionDrawer` / `ComputeDrawer` / `SettingsDrawer` / `ChartStatusBar` 内容，**不改它们的 props/emit**
- 中文注释；测试中文 describe/it
- 项目源码不加 `-cc` 后缀；文件内容不署名工具
- 浅色主题为主
- 单 PR 推进，全部 17 个 task 走完后跑一次完整验收

**命令注意**：
- 本机无 `pnpm`，全部用 `npx` / `npm`：
  - 跑单文件 vitest：`npx vitest run <path>`
  - 全量测试：`npm test`
  - verify:domain：`npm run verify:domain`
  - 启动 dev：`npm run dev`
  - 全量构建走：`npm run check:data && npm run verify:domain && npx vite build`（`npm run build` 内部硬编码 `pnpm`，会失败）
- 项目无 git，**不执行 `git add` / `git commit`**；每个 task 用 `npm test`（必要时加 `npx vitest run <path>` 做 checkpoint）

---

## 任务前置说明

- **Pinia setup store 字段访问形式**：`useLabStore()` 返回的对象在 setup store 中已 unwrap，组件 / 测试中读 `lab.leftPanelOpen`、`lab.activeLeftTab`，**不要 `.value`**。在测试中可直接 `lab.toggleLeftPanel()`。
- **删除文件用回收站**：本机是 Windows，凡是删文件都用 `powershell -Command "Add-Type -AssemblyName Microsoft.VisualBasic; [Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('完整路径', 'OnlyErrorDialogs', 'SendToRecycleBin')"`，禁止 `rm` / `Remove-Item -Force`。
- **localStorage 兼容**：旧 `lab.activeDrawer.v1` 不主动清理（`persistedRef` 字段级合并已自动忽略不识别字段）；新增三个 key：`lab.leftPanelOpen.v1` / `lab.rightPanelOpen.v1` / `lab.activeLeftTab.v1`。
- **inferTdpy 已存在**：在 `src/domain/market/tdpy.js`，`inferTdpy(sample).basis` 取值 `'crypto'` / `'us'` / `'hk'` / `'cn'` / `'fallback'`。RightPanel 直接消费。
- **不写 LeftPanel / RightPanel 组件单测**（spec §7 已确认，性价比低，靠手动验收 + e2e 视觉）。
- **`*Drawer` 命名保留**：spec §10 YAGNI 明确不重命名为 `*Tab`。

---

## Task 1：`usePlanning.js` 删抽屉单例 + 加面板态

**Files:**
- Modify: `src/composables/usePlanning.js`

- [ ] **Step 1.1：替换 activeDrawer 三件套为面板态**

打开 `src/composables/usePlanning.js`，把第 60-76 行（含 `activeDrawer` 注释、声明、`openDrawer` / `closeDrawer` 函数和 `return` 中三个字段）替换为：

替换前（第 60-63 行附近）：
```js
  // 抽屉单例：'decision'|'compute'|'settings'|null
  const activeDrawer = persistedRef('lab.activeDrawer.v1', null)
  function openDrawer(name) { activeDrawer.value = name }
  function closeDrawer() { activeDrawer.value = null }
```

替换后：
```js
  // 三栏面板状态：左/右面板开闭 + 左面板当前 tab
  const leftPanelOpen = persistedRef('lab.leftPanelOpen.v1', true)
  const rightPanelOpen = persistedRef('lab.rightPanelOpen.v1', true)
  const activeLeftTab = persistedRef('lab.activeLeftTab.v1', 'decision')

  function toggleLeftPanel() { leftPanelOpen.value = !leftPanelOpen.value }
  function toggleRightPanel() { rightPanelOpen.value = !rightPanelOpen.value }
```

并把 `return` 块中的：
```js
    activeDrawer,
    openDrawer,
    closeDrawer,
```
替换为：
```js
    leftPanelOpen,
    rightPanelOpen,
    activeLeftTab,
    toggleLeftPanel,
    toggleRightPanel,
```

- [ ] **Step 1.2：更新文件顶部 JSDoc 注释**

把第 6-15 行的注释块改为：

```js
/**
 * 输入参数 + UI 选中态层
 *
 * v3.1 改造：
 *   - 删除 activeDrawer / openDrawer / closeDrawer（三栏布局不再用单例抽屉）
 *   - 新增 leftPanelOpen / rightPanelOpen / activeLeftTab + toggle 函数
 *   - 保留 input / tdpyOverride / activeFormulaId / strategyProfileList
 */
```

- [ ] **Step 1.3：跑 vitest 单文件确认 usePlanning 不被直接覆盖（应该全部通过，因为没有针对它的单测）**

Run: `npx vitest run src/composables/__tests__/`
Expected: PASS（仅 useChartOverlays 测试，应仍 6/6 通过）

---

## Task 2：`labStore.js` return 同步增删

**Files:**
- Modify: `src/stores/labStore.js`

- [ ] **Step 2.1：修改 return 中"抽屉与主图叠加"段**

定位到第 131-135 行：

替换前：
```js
    // 抽屉与主图叠加（v3 新增）
    activeDrawer: planning.activeDrawer,
    openDrawer: planning.openDrawer,
    closeDrawer: planning.closeDrawer,
    chartOverlays,
```

替换后：
```js
    // 三栏面板态 + 主图叠加（v3.1）
    leftPanelOpen: planning.leftPanelOpen,
    rightPanelOpen: planning.rightPanelOpen,
    activeLeftTab: planning.activeLeftTab,
    toggleLeftPanel: planning.toggleLeftPanel,
    toggleRightPanel: planning.toggleRightPanel,
    chartOverlays,
```

- [ ] **Step 2.2：跑 store 测试看红（预期挂在"对外 API 字段齐全"和"openDrawer / closeDrawer 切换"两个用例）**

Run: `npx vitest run src/stores/__tests__/labStore.test.js`
Expected: FAIL（"对外 API 字段齐全"、"openDrawer / closeDrawer 切换 activeDrawer" 两个用例失败）

> 失败是预期的；下一个 task 修测试。

---

## Task 3：`labStore.test.js` 删旧测 + 加新测

**Files:**
- Modify: `src/stores/__tests__/labStore.test.js`

- [ ] **Step 3.1：修改"对外 API 字段齐全"用例**

定位到第 32-37 行（`// 抽屉与主图叠加（v3）` 段）：

替换前：
```js
    // 抽屉与主图叠加（v3）
    expect(lab.activeDrawer).toBeDefined()
    expect(typeof lab.openDrawer).toBe('function')
    expect(typeof lab.closeDrawer).toBe('function')
    expect(lab.chartOverlays).toBeDefined()
    expect(lab.chartOverlays.costBand).toBe(true)
```

替换后：
```js
    // 三栏面板态 + 主图叠加（v3.1）
    expect(lab.leftPanelOpen).toBe(true)
    expect(lab.rightPanelOpen).toBe(true)
    expect(lab.activeLeftTab).toBe('decision')
    expect(typeof lab.toggleLeftPanel).toBe('function')
    expect(typeof lab.toggleRightPanel).toBe('function')
    expect(lab.chartOverlays).toBeDefined()
    expect(lab.chartOverlays.costBand).toBe(true)
```

- [ ] **Step 3.2：删掉旧抽屉用例**

定位到第 109-118 行整个 `it('openDrawer / closeDrawer 切换 activeDrawer', ...)` 块，**整体删除**（连同前后空行保持一个空行分隔）。

- [ ] **Step 3.3：在删除位置插入两个新用例**

在原"openDrawer/closeDrawer"块的位置，插入：

```js
  it('toggleLeftPanel / toggleRightPanel 切换面板态并持久化', () => {
    const lab = useLabStore()
    expect(lab.leftPanelOpen).toBe(true)
    lab.toggleLeftPanel()
    expect(lab.leftPanelOpen).toBe(false)
    lab.toggleLeftPanel()
    expect(lab.leftPanelOpen).toBe(true)

    expect(lab.rightPanelOpen).toBe(true)
    lab.toggleRightPanel()
    expect(lab.rightPanelOpen).toBe(false)
  })

  it('activeLeftTab 默认 decision，可切到 compute / settings', () => {
    const lab = useLabStore()
    expect(lab.activeLeftTab).toBe('decision')
    lab.activeLeftTab = 'compute'
    expect(lab.activeLeftTab).toBe('compute')
    lab.activeLeftTab = 'settings'
    expect(lab.activeLeftTab).toBe('settings')
  })
```

- [ ] **Step 3.4：跑测试确认 store 套件全过**

Run: `npx vitest run src/stores/__tests__/labStore.test.js`
Expected: PASS · 9 个用例（原 8 个："对外 API"、"初始 input"、"importText"、"A3 回归"、"effectiveTdpy 默认按 source"、"用户覆盖优先"、"openDrawer/closeDrawer"、"chartOverlays 单字段"；删 1 加 2 = 9 个）全过

- [ ] **Step 3.5：跑全量测试确认整体一致**

Run: `npm test`
Expected: PASS · 全量从 62 → 63（原 62 - 1 + 2 = 63）

> 如果总数不对，回到 Step 3.3 检查是否漏插或多插用例。

---

## Task 4：新建 `LeftPanel.vue`

**Files:**
- Create: `src/components/LeftPanel.vue`

> 设计原则：LeftPanel 不持有状态，全部通过 props 进、emit 出；3 个 tab 内嵌已有 `*Drawer` 组件，事件透传即可。

- [ ] **Step 4.1：创建文件**

`src/components/LeftPanel.vue` 全文如下：

```vue
<script setup>
import { computed } from 'vue'
import DecisionDrawer from './DecisionDrawer.vue'
import ComputeDrawer from './ComputeDrawer.vue'
import SettingsDrawer from './SettingsDrawer.vue'

const props = defineProps({
  open: { type: Boolean, required: true },
  activeTab: { type: String, required: true }, // 'decision'|'compute'|'settings'
  // 整体 lab 引用，避免大量 props 透传
  lab: { type: Object, required: true },
  theme: { type: String, default: 'light' },
})

const emit = defineEmits([
  'toggle',
  'set-tab',
  // 透传给三个 *Drawer
  'set-profile',
  'set-auto-profile',
  'select-formula',
  'override-tdpy',
  'reset-tdpy',
  'set-theme',
  'reset-all',
])

const TAB_LABELS = {
  decision: '💡 决策',
  compute:  '📊 计算',
  settings: '⚙️ 设置',
}

const collapsedLabel = computed(() => TAB_LABELS[props.activeTab] || '面板')
</script>

<template>
  <aside class="lp" :class="{ open, collapsed: !open }">
    <!-- 折叠态：纯窄边按钮，点击展开 -->
    <button
      v-if="!open"
      type="button"
      class="lp-edge"
      :title="`展开 ${collapsedLabel}`"
      @click="emit('toggle')"
    >
      <span class="lp-edge-icon">▶</span>
      <span class="lp-edge-text">{{ collapsedLabel }}</span>
    </button>

    <!-- 展开态 -->
    <template v-else>
      <header class="lp-head">
        <nav class="lp-tabs" role="tablist">
          <button
            type="button"
            :class="{ active: activeTab === 'decision' }"
            role="tab"
            :aria-selected="activeTab === 'decision'"
            @click="emit('set-tab', 'decision')"
          >💡 决策</button>
          <button
            type="button"
            :class="{ active: activeTab === 'compute' }"
            role="tab"
            :aria-selected="activeTab === 'compute'"
            @click="emit('set-tab', 'compute')"
          >📊 计算</button>
          <button
            type="button"
            :class="{ active: activeTab === 'settings' }"
            role="tab"
            :aria-selected="activeTab === 'settings'"
            @click="emit('set-tab', 'settings')"
          >⚙️ 设置</button>
        </nav>
        <button class="lp-collapse" type="button" title="收起" @click="emit('toggle')">◀</button>
      </header>

      <div class="lp-body">
        <DecisionDrawer
          v-if="activeTab === 'decision'"
          :graph="lab.graph"
          :market="lab.market"
          :replay="lab.replay"
          :profile-replays="lab.profileReplays"
          :active-profile-id="lab.effectiveInput.strategyProfile"
          :auto-profile="lab.input.autoProfile"
          :profile-list="lab.strategyProfileList"
          @set-profile="(id) => emit('set-profile', id)"
          @set-auto-profile="(v) => emit('set-auto-profile', v)"
        />
        <ComputeDrawer
          v-else-if="activeTab === 'compute'"
          :graph="lab.graph"
          :market="lab.market"
          :rows="lab.rows"
          :cost-path="lab.costPath"
          :source-label="lab.sourceLabel"
          :active-formula-id="lab.activeFormulaId"
          :active-formula="lab.activeFormula"
          @select-formula="(id) => emit('select-formula', id)"
        />
        <SettingsDrawer
          v-else-if="activeTab === 'settings'"
          :input="lab.input"
          :tdpy-meta="lab.tdpyMeta"
          :effective-tdpy="lab.effectiveTdpy"
          :symbol="lab.source?.symbol ?? ''"
          :overlays="lab.chartOverlays"
          :theme="theme"
          @override-tdpy="(sym, val) => emit('override-tdpy', sym, val)"
          @reset-tdpy="(sym) => emit('reset-tdpy', sym)"
          @set-theme="(t) => emit('set-theme', t)"
          @reset-all="emit('reset-all')"
        />
      </div>
    </template>
  </aside>
</template>

<style>
.lp { height: 100%; min-height: 0; display: flex; flex-direction: column; background: var(--panel); border-right: 1px solid var(--line); overflow: hidden; }

/* 折叠态：竖排窄按钮 */
.lp-edge { width: 100%; height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: flex-start; gap: 8px; padding: 12px 0; border: none; background: transparent; color: var(--ink); cursor: pointer; }
.lp-edge:hover { background: var(--surface-active); color: var(--green); }
.lp-edge-icon { font-size: 0.78rem; }
.lp-edge-text { writing-mode: vertical-rl; font-size: 0.74rem; font-weight: 800; letter-spacing: 0.06em; }

/* 展开态 */
.lp-head { display: flex; align-items: center; justify-content: space-between; gap: 6px; padding: 7px 8px 7px 10px; border-bottom: 1px solid var(--line); flex-shrink: 0; }
.lp-tabs { display: flex; gap: 4px; flex: 1; min-width: 0; }
.lp-tabs button { flex: 1; min-width: 0; min-height: 26px; padding: 1px 6px; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.74rem; font-weight: 700; cursor: pointer; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.lp-tabs button:hover { border-color: var(--green); }
.lp-tabs button.active { background: var(--surface-active); border-color: var(--green); color: var(--green); }
.lp-collapse { width: 24px; height: 24px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.72rem; cursor: pointer; flex-shrink: 0; }
.lp-collapse:hover { border-color: var(--green); color: var(--green); }
.lp-body { flex: 1; min-height: 0; overflow-y: auto; padding: 10px 12px; }
</style>
```

- [ ] **Step 4.2：跑全量测试确认未破坏既有用例（LeftPanel 不在测试套件，应保持 PASS）**

Run: `npm test`
Expected: PASS · 63 个测试通过

---

## Task 5：新建 `RightPanel.vue`

**Files:**
- Create: `src/components/RightPanel.vue`

> 设计要点：用 `inferTdpy(sample).basis` 把 samples 分 4 组；filter 是组件内本地状态；当前 source 所属组默认展开，否则展开 `crypto`。

- [ ] **Step 5.1：创建文件**

`src/components/RightPanel.vue` 全文如下：

```vue
<script setup>
import { computed, ref } from 'vue'
import { inferTdpy } from '../domain/market/tdpy.js'

const props = defineProps({
  open: { type: Boolean, required: true },
  samples: { type: Array, required: true },
  currentSource: { type: Object, default: null },
  loadingSampleId: { type: String, default: '' },
})

const emit = defineEmits(['toggle', 'select-sample'])

const filter = ref('')

const GROUP_META = [
  { id: 'crypto', label: '加密', icon: '🪙' },
  { id: 'us',     label: '美股', icon: '📈' },
  { id: 'hk',     label: '港股', icon: '🇭🇰' },
  { id: 'cn',     label: 'A 股', icon: '🇨🇳' },
]

// 按市场分组（不含 fallback：fallback 多为未识别样本，不显示）
const groupedSamples = computed(() => {
  const buckets = { crypto: [], us: [], hk: [], cn: [] }
  for (const s of props.samples) {
    const basis = inferTdpy(s).basis
    if (basis in buckets) buckets[basis].push(s)
  }
  return GROUP_META
    .map((g) => ({ ...g, items: buckets[g.id] }))
    .filter((g) => g.items.length > 0)
})

// 默认展开组：当前 source 所属组优先；否则展开第一个非空组（一般是 crypto）
const defaultExpandedGroupId = computed(() => {
  const cur = props.currentSource
  if (cur) {
    const basis = inferTdpy(cur).basis
    if (groupedSamples.value.some((g) => g.id === basis)) return basis
  }
  return groupedSamples.value[0]?.id ?? null
})

// 过滤后的分组（按 symbol 或 label 不区分大小写包含匹配；空组隐藏）
const filteredGroups = computed(() => {
  const q = filter.value.trim().toLowerCase()
  if (!q) return groupedSamples.value
  return groupedSamples.value
    .map((g) => ({
      ...g,
      items: g.items.filter((s) =>
        String(s.symbol).toLowerCase().includes(q) ||
        String(s.label).toLowerCase().includes(q)
      ),
    }))
    .filter((g) => g.items.length > 0)
})

function onSelect(s) {
  emit('select-sample', s)
}
</script>

<template>
  <aside class="rp" :class="{ open, collapsed: !open }">
    <!-- 折叠态 -->
    <button
      v-if="!open"
      type="button"
      class="rp-edge"
      title="展开市场列表"
      @click="emit('toggle')"
    >
      <span class="rp-edge-icon">◀</span>
      <span class="rp-edge-text">市场</span>
    </button>

    <!-- 展开态 -->
    <template v-else>
      <header class="rp-head">
        <input
          v-model="filter"
          type="search"
          class="rp-filter"
          placeholder="过滤代码/名称…"
        />
        <button class="rp-collapse" type="button" title="收起" @click="emit('toggle')">▶</button>
      </header>

      <div class="rp-body">
        <p v-if="!filteredGroups.length" class="rp-empty">无匹配标的</p>
        <details
          v-for="g in filteredGroups"
          :key="g.id"
          :open="g.id === defaultExpandedGroupId || filter.length > 0"
          class="rp-group"
        >
          <summary>
            <span class="rp-g-label">{{ g.icon }} {{ g.label }}</span>
            <em class="rp-g-count">{{ g.items.length }}</em>
          </summary>
          <ul>
            <li
              v-for="s in g.items"
              :key="s.id"
              :class="{
                active: currentSource?.id === s.id,
                loading: loadingSampleId === s.id,
              }"
              @click="onSelect(s)"
            >
              <span class="sym">{{ s.symbol }}</span>
              <span v-if="s.label !== s.symbol" class="lbl">{{ s.label }}</span>
              <span v-if="loadingSampleId === s.id" class="loader">载入中…</span>
            </li>
          </ul>
        </details>
      </div>
    </template>
  </aside>
</template>

<style>
.rp { height: 100%; min-height: 0; display: flex; flex-direction: column; background: var(--panel); border-left: 1px solid var(--line); overflow: hidden; }

/* 折叠态 */
.rp-edge { width: 100%; height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: flex-start; gap: 8px; padding: 12px 0; border: none; background: transparent; color: var(--ink); cursor: pointer; }
.rp-edge:hover { background: var(--surface-active); color: var(--green); }
.rp-edge-icon { font-size: 0.78rem; }
.rp-edge-text { writing-mode: vertical-rl; font-size: 0.74rem; font-weight: 800; letter-spacing: 0.06em; }

/* 展开态 head */
.rp-head { display: flex; align-items: center; gap: 6px; padding: 7px 8px 7px 10px; border-bottom: 1px solid var(--line); flex-shrink: 0; }
.rp-filter { flex: 1; min-width: 0; min-height: 24px; padding: 2px 7px; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.74rem; }
.rp-filter:focus { outline: none; border-color: var(--green); }
.rp-collapse { width: 24px; height: 24px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 4px; background: var(--bg); color: var(--ink); font-size: 0.72rem; cursor: pointer; flex-shrink: 0; }
.rp-collapse:hover { border-color: var(--green); color: var(--green); }

/* 展开态 body */
.rp-body { flex: 1; min-height: 0; overflow-y: auto; padding: 6px 8px 10px; }
.rp-empty { margin: 12px 4px; padding: 14px; color: var(--muted); font-size: 0.78rem; text-align: center; border: 1px dashed var(--line); border-radius: 6px; }
.rp-group { margin: 4px 0; border: 1px solid var(--line); border-radius: 5px; background: var(--bg); }
.rp-group > summary { display: flex; justify-content: space-between; align-items: center; padding: 6px 9px; font-size: 0.78rem; font-weight: 800; cursor: pointer; list-style: none; }
.rp-group > summary::-webkit-details-marker { display: none; }
.rp-g-label { line-height: 1.2; }
.rp-g-count { font-style: normal; color: var(--muted); font-size: 0.7rem; font-variant-numeric: tabular-nums; }
.rp-group ul { list-style: none; margin: 0; padding: 2px 4px 6px; }
.rp-group li { display: grid; grid-template-columns: auto 1fr auto; gap: 6px; align-items: baseline; padding: 5px 8px; border-radius: 4px; cursor: pointer; font-size: 0.74rem; }
.rp-group li:hover { background: var(--surface-active); }
.rp-group li.active { background: var(--surface-active); border-left: 2px solid var(--green); padding-left: 6px; color: var(--green); font-weight: 800; }
.rp-group li.loading { opacity: 0.6; cursor: wait; }
.rp-group li .sym { font-weight: 800; font-variant-numeric: tabular-nums; }
.rp-group li .lbl { color: var(--muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.rp-group li .loader { color: var(--muted); font-size: 0.66rem; }
</style>
```

- [ ] **Step 5.2：跑全量测试**

Run: `npm test`
Expected: PASS · 63 个测试通过（RightPanel 无单测）

---

## Task 6：`MainChart.vue` 删 ChartFabs 引用

**Files:**
- Modify: `src/components/MainChart.vue`

- [ ] **Step 6.1：删 import**

第 13 行：
```js
import ChartFabs from './ChartFabs.vue'
```
**整行删除**。

- [ ] **Step 6.2：删 prop `activeDrawer`**

第 26 行（在 `defineProps` 末尾）：
```js
  activeDrawer: { type: String, default: null },
```
**整行删除**（注意保留前一行末尾的逗号判断；如果 `activeDrawer` 是最后一项，删后逗号无影响）。

- [ ] **Step 6.3：删 emit `'open-drawer'`**

第 29 行：

替换前：
```js
const emit = defineEmits(['cursor-change', 'param-change', 'open-drawer'])
```
替换后：
```js
const emit = defineEmits(['cursor-change', 'param-change'])
```

- [ ] **Step 6.4：删模板中 `<ChartFabs />`**

定位到模板段（约第 292 行）：
```vue
    <ChartFabs :active-drawer="activeDrawer" @open="(name) => emit('open-drawer', name)" />
```
**整行删除**。

- [ ] **Step 6.5：跑测试**

Run: `npm test`
Expected: PASS · 63 个测试通过

> MainChart 没有针对 props.activeDrawer / emit('open-drawer') 的单测，纯 UI 改动。

---

## Task 7：`TopBar.vue` 删搜索

**Files:**
- Modify: `src/components/TopBar.vue`

- [ ] **Step 7.1：删 import 中无用的 `ref`（保留 `computed`）**

第 2 行：
```js
import { computed, ref } from 'vue'
```
替换为：
```js
import { computed } from 'vue'
```

- [ ] **Step 7.2：删 `samples` / `loadingSampleId` props**

定位到 `defineProps` 末尾：
```js
  samples: { type: Array, required: true },
  loadingSampleId: { type: String, default: '' },
```
**整两行删除**（同时把上一行 `theme: { type: String, default: 'light' },` 末尾的逗号保留，避免破坏对象字面量）。

- [ ] **Step 7.3：删 `'select-sample'` emit**

定位到 `defineEmits`：

替换前：
```js
const emit = defineEmits([
  'select-sample',
  'set-profile',
  'set-auto-profile',
  'toggle-theme',
  'reset',
])
```
替换后：
```js
const emit = defineEmits([
  'set-profile',
  'set-auto-profile',
  'toggle-theme',
  'reset',
])
```

- [ ] **Step 7.4：删搜索逻辑（ref + computed + handler）**

把第 31-44 行整段删除：
```js
const tickerSearch = ref('')
const searchInput = ref(null)

const filteredSamples = computed(() => {
  const q = tickerSearch.value.trim().toLowerCase()
  if (!q) return props.samples
  return props.samples.filter(s =>
    s.symbol.toLowerCase().includes(q) || s.label.toLowerCase().includes(q)
  )
})

function onSearchEnter() {
  if (filteredSamples.value.length) emit('select-sample', filteredSamples.value[0])
}
```

- [ ] **Step 7.5：删模板中 `.tb-search` 块**

定位模板（约第 88-100 行整段）：
```vue
    <div class="tb-search">
      <input
        ref="searchInput"
        v-model="tickerSearch"
        type="search"
        placeholder="搜代码/名称…"
        list="ticker-list"
        @keydown.enter="onSearchEnter"
      />
      <datalist id="ticker-list">
        <option v-for="s in samples" :key="s.id" :value="s.symbol" />
      </datalist>
    </div>
```
**整段删除**。

- [ ] **Step 7.6：grid template-columns 改 3 列**

定位 `.topbar` 样式（约第 134 行）：

替换前：
```css
.topbar { display: grid; grid-template-columns: auto auto 1fr auto; gap: 14px; align-items: center; padding: 7px 14px; background: var(--panel); border-bottom: 1px solid var(--line); flex-shrink: 0; }
```
替换后：
```css
.topbar { display: grid; grid-template-columns: auto 1fr auto; gap: 14px; align-items: center; padding: 7px 14px; background: var(--panel); border-bottom: 1px solid var(--line); flex-shrink: 0; }
```

- [ ] **Step 7.7：删 `.tb-search` 相关 CSS**

定位并删除：
```css
.tb-search input { width: 200px; min-height: 28px; padding: 3px 8px; border: 1px solid var(--line); border-radius: 5px; background: var(--bg); color: var(--ink); font-size: 0.74rem; }
```

并修改窄屏 media query（约第 159-162 行）：

替换前：
```css
@media (max-width: 800px) {
  .tb-summary .tb-narrative { display: none; }
  .tb-search input { width: 140px; }
}
```
替换后：
```css
@media (max-width: 800px) {
  .tb-summary .tb-narrative { display: none; }
}
```

- [ ] **Step 7.8：跑测试**

Run: `npm test`
Expected: PASS · 63 个测试通过

---

## Task 8：`App.vue` 改三栏 grid

**Files:**
- Modify: `src/App.vue`

> 注：本 task 是改动最大的一个，需要重写大半个文件。每一步都给完整代码片段。

- [ ] **Step 8.1：替换 `<script setup>` 中 import**

定位文件第 1-12 行：

替换前：
```js
<script setup>
import { computed, watch } from 'vue'
import { Activity } from 'lucide-vue-next'
import TopBar from './components/TopBar.vue'
import MainChart from './components/MainChart.vue'
import DrawerHost from './components/DrawerHost.vue'
import DecisionDrawer from './components/DecisionDrawer.vue'
import ComputeDrawer from './components/ComputeDrawer.vue'
import SettingsDrawer from './components/SettingsDrawer.vue'
import { useLabStore } from './stores/labStore.js'
import { clearPersistedLab, persistedRef } from './composables/usePersisted.js'
import stockIndex from './data/stock-index.json'
```

替换后：
```js
<script setup>
import { computed, watch } from 'vue'
import { Activity } from 'lucide-vue-next'
import TopBar from './components/TopBar.vue'
import MainChart from './components/MainChart.vue'
import LeftPanel from './components/LeftPanel.vue'
import RightPanel from './components/RightPanel.vue'
import { useLabStore } from './stores/labStore.js'
import { clearPersistedLab, persistedRef } from './composables/usePersisted.js'
import stockIndex from './data/stock-index.json'
```

- [ ] **Step 8.2：替换整个 `<template>`**

定位 `<template>` 块（约第 59-156 行），整个替换为：

```vue
<template>
  <div
    class="app-root"
    :class="{
      'left-collapsed': !lab.leftPanelOpen,
      'right-collapsed': !lab.rightPanelOpen,
    }"
  >
    <TopBar
      :source="lab.source"
      :market="lab.market"
      :rows="lab.rows"
      :decision="lab.graph?.decision"
      :confidence="briefConfidence"
      :profile-id="lab.input.strategyProfile"
      :auto-profile="lab.input.autoProfile"
      :profile-list="lab.strategyProfileList"
      :recommended-id="lab.recommendedProfile?.id ?? 'balanced'"
      :theme="theme"
      @set-profile="onSetProfile"
      @set-auto-profile="onSetAutoProfile"
      @toggle-theme="toggleTheme"
      @reset="resetWorkbench"
    />

    <p v-if="lab.error" class="err-bar" :class="`kind-${lab.error.kind}`">
      <span class="err-msg">{{ lab.error.message }}</span>
      <button v-if="lab.error.sample" class="err-btn" @click="lab.retryLast()" :disabled="lab.loading">
        {{ lab.loading ? '重试中…' : '重试' }}
      </button>
      <button class="err-btn err-dismiss" @click="lab.dismissError()">关闭</button>
    </p>

    <div class="cols">
      <LeftPanel
        :open="lab.leftPanelOpen"
        :active-tab="lab.activeLeftTab"
        :lab="lab"
        :theme="theme"
        @toggle="lab.toggleLeftPanel"
        @set-tab="(name) => lab.activeLeftTab = name"
        @set-profile="onSetProfile"
        @set-auto-profile="onSetAutoProfile"
        @select-formula="(id) => lab.activeFormulaId = id"
        @override-tdpy="(sym, val) => lab.setTdpyOverride(sym, val)"
        @reset-tdpy="(sym) => lab.clearTdpyOverride(sym)"
        @set-theme="(t) => { theme = t }"
        @reset-all="resetWorkbench"
      />

      <main class="app-main">
        <MainChart
          v-if="lab.rows.length"
          :rows="lab.rows"
          :cost-path="lab.costPath"
          :formula-path="lab.formulaPath"
          :entry-price="lab.input.entryPrice"
          :replay="lab.replay"
          :market="lab.market"
          :decision="lab.graph?.decision"
          :overlays="lab.chartOverlays"
          :input="lab.input"
          @cursor-change="(idx) => { if (idx !== null) lab.cursor = idx }"
          @param-change="onParamChange"
        />
        <div v-else class="empty-state">
          <Activity :size="36" />
          <strong>Market Lab</strong>
          <small v-if="lab.loading">加载中…{{ lab.loadingSampleId ? ' ' + lab.loadingSampleId : '' }}</small>
          <small v-else>右侧标的列表选品种加载（首次约 1~2 秒）</small>
        </div>
      </main>

      <RightPanel
        :open="lab.rightPanelOpen"
        :samples="allSamples"
        :current-source="lab.source"
        :loading-sample-id="lab.loadingSampleId"
        @toggle="lab.toggleRightPanel"
        @select-sample="lab.loadSample"
      />
    </div>
  </div>
</template>
```

- [ ] **Step 8.3：替换 `<style>` 块**

定位 `<style>` 块（约第 158-176 行），整个替换为：

```vue
<style>
.app-root {
  width: 100vw; height: 100vh;
  display: flex; flex-direction: column; overflow: hidden;
  background: var(--bg); color: var(--ink);
  --left-w: 280px;
  --right-w: 240px;
}
.app-root.left-collapsed { --left-w: 36px; }
.app-root.right-collapsed { --right-w: 36px; }

.cols {
  flex: 1; min-height: 0;
  display: grid;
  grid-template-columns: var(--left-w) 1fr var(--right-w);
  transition: grid-template-columns 200ms ease;
}

.app-main { min-width: 0; min-height: 0; position: relative; overflow: hidden; }

.err-bar { flex-shrink: 0; display: flex; gap: 10px; align-items: center; margin: 0; padding: 5px 12px; background: var(--red); color: #fff; font-size: 0.76rem; }
.err-bar.kind-empty { background: #b8860b; }
.err-bar.kind-parse { background: #884d22; }
.err-bar.kind-network { background: var(--red); }
.err-msg { flex: 1; }
.err-btn { min-height: 22px; padding: 1px 9px; border: 1px solid rgba(255,255,255,0.5); border-radius: 4px; background: transparent; color: #fff; font-size: 0.7rem; font-weight: 800; cursor: pointer; }
.err-btn:hover:not(:disabled) { background: rgba(255,255,255,0.15); }
.err-btn:disabled { opacity: 0.55; cursor: not-allowed; }
.err-dismiss { opacity: 0.8; }

.empty-state { width: 100%; height: 100%; display: grid; place-items: center; align-content: center; gap: 10px; color: var(--muted); }
.empty-state strong { font-size: 1.3rem; color: var(--ink); }

@media (max-width: 900px) {
  .app-root { --left-w: 36px; --right-w: 36px; }
}
</style>
```

> 注：去掉了无用的 `.dh-todo`（DrawerHost 已删）。

- [ ] **Step 8.4：跑测试 + 启动 dev 视觉验证**

Run: `npm test`
Expected: PASS · 63 个测试通过

Run: `npm run dev`
Expected: 浏览器打开 `http://localhost:5173`，看到三栏（左 280px / 中间 / 右 240px），左面板默认显示"决策"tab，右面板默认展开"加密"组，TopBar 无搜索框。

> 视觉验证后 `Ctrl+C` 停掉 dev server。

---

## Task 9：删除孤儿组件 `ChartFabs.vue` + `DrawerHost.vue`

**Files:**
- Delete (回收站): `src/components/ChartFabs.vue`
- Delete (回收站): `src/components/DrawerHost.vue`

> 这两个文件已在 Task 6 + Task 8 中失去引用，搬到回收站。

- [ ] **Step 9.1：用 PowerShell 把 ChartFabs.vue 移到回收站**

```powershell
powershell -Command "Add-Type -AssemblyName Microsoft.VisualBasic; [Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('F:\devarea\market-lab-amplify\src\components\ChartFabs.vue', 'OnlyErrorDialogs', 'SendToRecycleBin')"
```

- [ ] **Step 9.2：用 PowerShell 把 DrawerHost.vue 移到回收站**

```powershell
powershell -Command "Add-Type -AssemblyName Microsoft.VisualBasic; [Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('F:\devarea\market-lab-amplify\src\components\DrawerHost.vue', 'OnlyErrorDialogs', 'SendToRecycleBin')"
```

- [ ] **Step 9.3：确认两文件已不在源码树**

Run (Bash):
```bash
ls F:/devarea/market-lab-amplify/src/components/ChartFabs.vue 2>&1 | grep -i "no such" && echo OK_CHARTFABS_GONE || echo FAIL_CHARTFABS_PRESENT
ls F:/devarea/market-lab-amplify/src/components/DrawerHost.vue 2>&1 | grep -i "no such" && echo OK_DRAWERHOST_GONE || echo FAIL_DRAWERHOST_PRESENT
```
Expected: 输出 `OK_CHARTFABS_GONE` 和 `OK_DRAWERHOST_GONE`

- [ ] **Step 9.4：grep 全源码确认没有遗漏 import**

Run:
```bash
npx vitest run --reporter=verbose 2>&1 | tail -20 || true
```

并用 grep（通过 Grep 工具）确认无残留：
- 模式：`ChartFabs|DrawerHost`
- 路径：`src/`
- 期望：无任何 `.vue` / `.js` 文件命中（如果命中，回查并修掉）

- [ ] **Step 9.5：跑测试 + 跑构建**

Run: `npm test`
Expected: PASS · 63 个测试通过

Run: `npm run check:data && npm run verify:domain && npx vite build`
Expected: 构建成功，无 import 报错

---

## Task 10：最终验收 + 手动 checklist

**Files:**
- 无修改

> 走 spec §11 的 14 项验收清单。任何一项不过，回到对应 task 修。

- [ ] **Step 10.1：清空 localStorage 后启动 dev**

```bash
# 浏览器打开 DevTools → Application → Local Storage → 清空 lab.* 键
# 或直接换匿名窗口
```

Run: `npm run dev`

- [ ] **Step 10.2：手动验收 14 项**

按下表逐项打钩（在浏览器里操作）：

| # | 操作 | 期望 | ✅ |
|---|---|---|---|
| 1 | 首次加载 | 三栏：左面板展开（决策 tab）/ 主图 / 右面板展开（市场列表） | ☐ |
| 2 | 右面板分组 | 加密 / 美股 / 港股 / A 股 4 个 group，初始展开"加密"或当前 source 所属组 | ☐ |
| 3 | 点右面板某标的 | `lab.loadSample` 触发，列表对应项 active 高亮（绿色边） | ☐ |
| 4 | 右面板过滤"AAPL" | 仅美股组显示 AAPL，其他组隐藏 | ☐ |
| 5 | 右面板过滤"xxxxx" | 显示"无匹配标的" | ☐ |
| 6 | 点右面板 ▶ 收起 | 右面板宽度 240→36，主图 resize 占更多空间 | ☐ |
| 7 | 点右面板 ◀ 展开 | 恢复 240，主图缩回 | ☐ |
| 8 | 左面板顶部 3 tab 切换 | 内容变（decision/compute/settings 各自子组件） | ☐ |
| 9 | 左面板 ◀ 收起 | 宽度 280→36，窄边条显示当前 tab 名（竖排） | ☐ |
| 10 | 左面板 ▶ 展开 | 恢复 280 + 当前 tab 内容 | ☐ |
| 11 | TopBar | 无搜索框；只剩 Logo / 决策摘要 / ProfileChip / 主题 / 重置 | ☐ |
| 12 | 主图 | 无 FAB；底部 4 chip 仍可点弹 popover 改值 | ☐ |
| 13 | 切深色主题 + 刷新 | 主题保留，面板态保留 | ☐ |
| 14 | 设置 tab 点重置 | confirm 后清空持久化 + 刷新 | ☐ |
| 15 | 浏览器宽度 < 900px | 两侧面板自动 collapse 到 36px | ☐ |

- [ ] **Step 10.3：跑全量回归**

Run: `npm test`
Expected: PASS · 63 passed

Run: `npm run verify:domain`
Expected: PASS · domain 公式数值零回归

Run: `npm run check:data && npx vite build`
Expected: 构建成功

- [ ] **Step 10.4：清理 `.dh-todo`（如果还残留）+ 关闭 dev**

> 已在 Task 8.3 删除，无需额外操作。

至此 v3.1 三栏改造完成。

---

## 自审清单

### Spec 覆盖检查

对照 `docs/superpowers/specs/2026-05-15-lab-v3.1-three-panel-design-cc.md` 各章节：

| Spec 章节 | 对应 Task |
|---|---|
| §3.1 usePlanning 改 | Task 1 |
| §3.2 labStore return | Task 2 |
| §3.3 labStore.test 改 | Task 3 |
| §4.1 LeftPanel 新建 | Task 4 |
| §4.2 RightPanel 新建 | Task 5 |
| §4.3 MainChart 删 ChartFabs | Task 6 |
| §4.4 TopBar 删搜索 | Task 7 |
| §4.5 App.vue 三栏 | Task 8 |
| §8 文件结构（含 ChartFabs/DrawerHost 删） | Task 9 |
| §11 验收清单 | Task 10 |

无遗漏。

### 占位符扫描

无 TBD / TODO / "实现后再说" / "类似 task N" 等占位符。所有代码片段完整可粘贴。

### 类型一致性检查

- LeftPanel emit 名 (`set-tab`, `set-profile` 等) 在 Task 4 和 Task 8 一致
- RightPanel emit 名 (`toggle`, `select-sample`) 在 Task 5 和 Task 8 一致
- store 字段名 (`leftPanelOpen` / `rightPanelOpen` / `activeLeftTab` / `toggleLeftPanel` / `toggleRightPanel`) 在 Task 1 / 2 / 3 / 8 全部一致
- localStorage key (`lab.leftPanelOpen.v1` / `lab.rightPanelOpen.v1` / `lab.activeLeftTab.v1`) 在 Task 1 唯一定义，无冲突
- `inferTdpy(s).basis` 取值 (`crypto`/`us`/`hk`/`cn`) 在 Task 5 与 `src/domain/market/tdpy.js` 一致

无类型/命名冲突。

### 风险确认

- **CSS grid 列宽切换 + lightweight-charts ResizeObserver 联动**：MainChart 在 v3 task 12 已加 `new ResizeObserver(() => resize())`，无需额外改。Task 8 已确保 `.app-main { min-width: 0; min-height: 0; }` 让 grid 收缩生效。
- **首次加载持久化 hydration**：`persistedRef` 已经 SSR-safe（typeof window 检查），无闪烁风险。
- **DecisionDrawer / ComputeDrawer / SettingsDrawer 在 LeftPanel 内的 props 透传**：与 v3 App.vue 中 DrawerHost slot 内的 props 一致（已对照过 Task 13 实现），无 props 缺失。

---

## 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-05-15-lab-v3.1-three-panel-cc.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - 主线程派 fresh subagent 跑每个 task，task 间做 spec compliance + code quality 两阶段 review；任务最多但反馈最快、质量最稳。

**2. Inline Execution** - 在当前会话直接走 executing-plans 跑全部 task，到关键节点暂停 checkpoint。

**Which approach?**
