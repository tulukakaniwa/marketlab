# Market Lab v3.2 · 拖宽 + KDJ/RSI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v3.1 三栏基础上加 ① 左右面板边框拖宽（持久化）② K 线下方 KDJ + RSI 两个 pane（默认开）。零 domain 数值回归。

**Architecture:** App.vue 加 4px 拖手 div + mousemove 节流（rAF）；store 用 persistedRef 存 leftPanelW/rightPanelW + clamp setter；CSS grid template-columns 改读 store 的 CSS 变量。MainChart 加 KDJ/RSI 两组 LineSeries 和 PriceLine 阈值线；computeKDJ / computeRSI 在 domain/indicators 单独实现 + 单测。

**Tech Stack:** Vue 3 Composition API · Pinia setup store · vitest · lightweight-charts v5（pane + LineSeries + PriceLine）· requestAnimationFrame 节流

**Spec:** `docs/superpowers/specs/2026-05-15-lab-v3.2-resize-kdj-rsi-design-cc.md`

**约束**：
- domain 公式数值零回归（`npm run verify:domain` 必须过）
- 中文注释；中文 describe/it
- 项目源码不加 `-cc` 后缀；文件内容不署名工具
- 浅色主题为主
- 单 PR

**命令注意**：
- 本机无 `pnpm`，全部用 `npx` / `npm`
- 项目无 `git`，**不**执行任何 `git add` / `git commit`
- 全量构建：`npm run check:data && npm run verify:domain && npx vite build`

**Task 拆分（10 个 task）**：
1. computeKDJ + 单测（domain）
2. computeRSI + 单测（domain）
3. useChartOverlays 加 kdjPane/rsiPane defaults + 测试
4. usePlanning 加 leftPanelW/rightPanelW + setter/reset
5. labStore.js return 同步 + 单测
6. ChartOverlayToggles.vue 加两项
7. MainChart.vue 加 KDJ pane（series + setData + hlines + rebalance）
8. MainChart.vue 加 RSI pane
9. App.vue 接拖手（mousemove + rAF + clamp + 双击重置 + CSS）
10. 最终验收

---

## Task 1：`computeKDJ` 公式 + 单测

**Files:**
- Create: `src/domain/indicators/kdj.js`
- Create: `src/domain/indicators/__tests__/kdj.test.js`

> KDJ 公式：rsv = 100 * (close - lowest_n) / (highest_n - lowest_n)；K = RMA(rsv, sig)；D = RMA(K, sig)；J = 3K - 2D。RMA = Wilder's MA = EMA(alpha = 1/sig)。

- [ ] **Step 1.1：创建目录**

```bash
mkdir -p src/domain/indicators/__tests__
```

- [ ] **Step 1.2：写失败测试 `kdj.test.js`**

```js
import { describe, expect, it } from 'vitest'
import { computeKDJ } from '../kdj.js'

describe('computeKDJ', () => {
  function rowsFromCloses(closes) {
    return closes.map((c, i) => ({
      date: `2024-01-${String(i + 1).padStart(2, '0')}`,
      open: c, high: c + 0.5, low: c - 0.5, close: c, volume: 1000,
    }))
  }

  it('前 n-1 根返回 null（n=9 时前 8 根）', () => {
    const result = computeKDJ(rowsFromCloses([1, 2, 3, 4, 5, 6, 7, 8]))
    expect(result.length).toBe(8)
    for (const r of result) {
      expect(r.k).toBeNull()
      expect(r.d).toBeNull()
      expect(r.j).toBeNull()
    }
  })

  it('单调上涨：J 收敛于 100 以上', () => {
    const result = computeKDJ(rowsFromCloses(Array.from({ length: 30 }, (_, i) => 100 + i * 2)))
    const last = result.at(-1)
    expect(last.k).toBeGreaterThan(50)
    expect(last.j).toBeGreaterThanOrEqual(last.k)
  })

  it('单调下跌：J 跌到 0 以下', () => {
    const result = computeKDJ(rowsFromCloses(Array.from({ length: 30 }, (_, i) => 200 - i * 2)))
    const last = result.at(-1)
    expect(last.k).toBeLessThan(50)
    expect(last.j).toBeLessThanOrEqual(last.k)
  })

  it('横盘 div=0 时 rsv 走 50 兜底', () => {
    const flat = Array.from({ length: 20 }, () => 100)
    const rows = flat.map((c, i) => ({
      date: `2024-01-${String(i + 1).padStart(2, '0')}`,
      open: c, high: c, low: c, close: c, volume: 1000,
    }))
    const result = computeKDJ(rows)
    const last = result.at(-1)
    expect(last.k).toBeCloseTo(50, 1)
    expect(last.d).toBeCloseTo(50, 1)
    expect(last.j).toBeCloseTo(50, 1)
  })

  it('自定义 n / sig 参数生效', () => {
    const rows = rowsFromCloses(Array.from({ length: 20 }, (_, i) => 100 + i))
    const r1 = computeKDJ(rows, { n: 5, sig: 3 })
    const r2 = computeKDJ(rows, { n: 9, sig: 3 })
    // n=5 时第 4 根开始有值；n=9 时第 8 根才有
    expect(r1[5].k).not.toBeNull()
    expect(r2[5].k).toBeNull()
  })
})
```

- [ ] **Step 1.3：跑测试看红**

Run: `npx vitest run src/domain/indicators/__tests__/kdj.test.js`
Expected: FAIL（kdj.js 不存在）

- [ ] **Step 1.4：实现 `kdj.js`**

```js
/**
 * KDJ 指标 (Stochastic Oscillator + J 线)
 *
 * 公式：
 *   div = highest(high, n) - lowest(low, n)
 *   rsv = div === 0 ? 50 : 100 * (close - lowest) / div
 *   K = RMA(rsv, sig)
 *   D = RMA(K, sig)
 *   J = 3*K - 2*D
 *
 * RMA（Wilder 平滑均线）：alpha = 1/period；首值用 SMA(period) 初始化，无可用初始值前为 NaN。
 */
export function computeKDJ(rows, { n = 9, sig = 3 } = {}) {
  const len = rows.length
  if (!len) return []

  const rsv = new Array(len).fill(NaN)
  for (let i = n - 1; i < len; i += 1) {
    let hi = -Infinity
    let lo = Infinity
    for (let j = i - n + 1; j <= i; j += 1) {
      if (rows[j].high > hi) hi = rows[j].high
      if (rows[j].low < lo) lo = rows[j].low
    }
    const div = hi - lo
    rsv[i] = div === 0 ? 50 : 100 * (rows[i].close - lo) / div
  }

  const kArr = rmaSeries(rsv, sig)
  const dArr = rmaSeries(kArr, sig)

  return rows.map((row, i) => {
    const k = kArr[i]
    const d = dArr[i]
    if (!Number.isFinite(k) || !Number.isFinite(d)) {
      return { date: row.date, k: null, d: null, j: null }
    }
    return { date: row.date, k, d, j: 3 * k - 2 * d }
  })
}

/**
 * RMA = Wilder's smoothed moving average，alpha = 1/period
 * - 首个有效值（输入数组的第 period 个非 NaN 位置）用 SMA(period) 初始化
 * - 之后 rma[i] = alpha * x[i] + (1 - alpha) * rma[i-1]
 * - NaN 输入直接 propagate 为 NaN 输出
 */
function rmaSeries(values, period) {
  const out = new Array(values.length).fill(NaN)
  const alpha = 1 / period
  let prev = NaN
  let count = 0
  let sum = 0

  for (let i = 0; i < values.length; i += 1) {
    const v = values[i]
    if (!Number.isFinite(v)) continue

    if (Number.isFinite(prev)) {
      prev = alpha * v + (1 - alpha) * prev
      out[i] = prev
      continue
    }

    count += 1
    sum += v
    if (count >= period) {
      prev = sum / period
      out[i] = prev
    }
  }

  return out
}
```

- [ ] **Step 1.5：跑测试**

Run: `npx vitest run src/domain/indicators/__tests__/kdj.test.js`
Expected: PASS · 5 个用例

- [ ] **Step 1.6：跑全量回归**

Run: `npm test`
Expected: PASS · 63 + 5 = 68 passed

---

## Task 2：`computeRSI` 公式 + 单测

**Files:**
- Create: `src/domain/indicators/rsi.js`
- Create: `src/domain/indicators/__tests__/rsi.test.js`

> RSI = 100 - 100/(1 + avgGain/avgLoss)，其中 avgGain/avgLoss = RMA(period)。custom = (raw - 35) * 2，对齐你的 Pine 脚本。

- [ ] **Step 2.1：写失败测试**

```js
import { describe, expect, it } from 'vitest'
import { computeRSI } from '../rsi.js'

describe('computeRSI', () => {
  function rowsFromCloses(closes) {
    return closes.map((c, i) => ({
      date: `2024-01-${String(i + 1).padStart(2, '0')}`,
      open: c, high: c + 0.5, low: c - 0.5, close: c, volume: 1000,
    }))
  }

  it('前 n 根返回 null（n=14 时前 14 根）', () => {
    const result = computeRSI(rowsFromCloses(Array.from({ length: 14 }, (_, i) => 100 + i)))
    expect(result.length).toBe(14)
    for (const r of result) {
      expect(r.raw).toBeNull()
      expect(r.custom).toBeNull()
    }
  })

  it('单调上涨：raw 收敛 100', () => {
    const result = computeRSI(rowsFromCloses(Array.from({ length: 40 }, (_, i) => 100 + i)))
    const last = result.at(-1)
    expect(last.raw).toBeCloseTo(100, 0)
    expect(last.custom).toBeCloseTo((100 - 35) * 2, 0)
  })

  it('单调下跌：raw 收敛 0', () => {
    const result = computeRSI(rowsFromCloses(Array.from({ length: 40 }, (_, i) => 200 - i)))
    const last = result.at(-1)
    expect(last.raw).toBeCloseTo(0, 0)
    expect(last.custom).toBeCloseTo((0 - 35) * 2, 0)
  })

  it('横盘：raw=50（除零兜底） + custom=30', () => {
    // 注意：完全平 close → delta=0 → avgGain=0/avgLoss=0 → 0/0 兜底
    const result = computeRSI(rowsFromCloses(Array.from({ length: 30 }, () => 100)))
    const last = result.at(-1)
    expect(last.raw).toBeCloseTo(50, 1)
    expect(last.custom).toBeCloseTo(30, 1)
  })

  it('自定义 n 参数生效', () => {
    const rows = rowsFromCloses(Array.from({ length: 30 }, (_, i) => 100 + i))
    const r1 = computeRSI(rows, { n: 5 })
    const r2 = computeRSI(rows, { n: 14 })
    expect(r1[5].raw).not.toBeNull()
    expect(r2[5].raw).toBeNull()
  })
})
```

- [ ] **Step 2.2：跑测试看红**

Run: `npx vitest run src/domain/indicators/__tests__/rsi.test.js`
Expected: FAIL（rsi.js 不存在）

- [ ] **Step 2.3：实现 `rsi.js`**

```js
/**
 * RSI（相对强弱指数）+ 自定义缩放版
 *
 * 公式：
 *   delta[i] = close[i] - close[i-1]
 *   gain = max(delta, 0)
 *   loss = max(-delta, 0)
 *   avgGain = RMA(gain, n)；avgLoss = RMA(loss, n)
 *   raw = avgLoss === 0
 *     ? (avgGain === 0 ? 50 : 100)        // 全平：50；只涨不跌：100
 *     : 100 - 100 / (1 + avgGain / avgLoss)
 *
 * Custom 缩放（与 Pine 对齐）：custom = (raw - 35) * 2
 */
export function computeRSI(rows, { n = 14 } = {}) {
  const len = rows.length
  if (!len) return []

  const gains = new Array(len).fill(NaN)
  const losses = new Array(len).fill(NaN)
  for (let i = 1; i < len; i += 1) {
    const delta = rows[i].close - rows[i - 1].close
    gains[i] = delta > 0 ? delta : 0
    losses[i] = delta < 0 ? -delta : 0
  }

  const avgGain = rmaSeries(gains, n)
  const avgLoss = rmaSeries(losses, n)

  return rows.map((row, i) => {
    const g = avgGain[i]
    const l = avgLoss[i]
    if (!Number.isFinite(g) || !Number.isFinite(l)) {
      return { date: row.date, raw: null, custom: null }
    }
    let raw
    if (l === 0) raw = g === 0 ? 50 : 100
    else raw = 100 - 100 / (1 + g / l)
    return { date: row.date, raw, custom: (raw - 35) * 2 }
  })
}

function rmaSeries(values, period) {
  const out = new Array(values.length).fill(NaN)
  const alpha = 1 / period
  let prev = NaN
  let count = 0
  let sum = 0

  for (let i = 0; i < values.length; i += 1) {
    const v = values[i]
    if (!Number.isFinite(v)) continue

    if (Number.isFinite(prev)) {
      prev = alpha * v + (1 - alpha) * prev
      out[i] = prev
      continue
    }

    count += 1
    sum += v
    if (count >= period) {
      prev = sum / period
      out[i] = prev
    }
  }

  return out
}
```

- [ ] **Step 2.4：跑测试**

Run: `npx vitest run src/domain/indicators/__tests__/rsi.test.js`
Expected: PASS · 5 个用例

- [ ] **Step 2.5：跑全量回归**

Run: `npm test`
Expected: PASS · 68 + 5 = 73 passed

---

## Task 3：`useChartOverlays.js` 加 kdjPane/rsiPane defaults

**Files:**
- Modify: `src/composables/useChartOverlays.js`
- Modify: `src/composables/__tests__/useChartOverlays.test.js`

- [ ] **Step 3.1：扩 DEFAULTS**

`src/composables/useChartOverlays.js`：

替换前：
```js
const DEFAULTS = {
  costBand: true,
  entryLine: true,
  volBand: true,
  volume: true,
  replayMarkers: true,
  currentDecision: true,
  deltaPane: false,
  equityPane: false,
}
```

替换后：
```js
const DEFAULTS = {
  costBand: true,
  entryLine: true,
  volBand: true,
  volume: true,
  replayMarkers: true,
  currentDecision: true,
  deltaPane: false,
  equityPane: false,
  kdjPane: true,
  rsiPane: true,
}
```

并把开头注释中"8 项布尔值"改为"10 项布尔值"。

- [ ] **Step 3.2：扩测试**

`src/composables/__tests__/useChartOverlays.test.js` 中找到第一个 `it('返回 ... 默认值符合 spec', ...)`：

替换前（断言 8 个）：
```js
  it('返回 8 个开关字段，默认值符合 spec', () => {
    const o = useChartOverlays()
    expect(o.costBand).toBe(true)
    expect(o.entryLine).toBe(true)
    expect(o.volBand).toBe(true)
    expect(o.volume).toBe(true)
    expect(o.replayMarkers).toBe(true)
    expect(o.currentDecision).toBe(true)
    expect(o.deltaPane).toBe(false)
    expect(o.equityPane).toBe(false)
  })
```

替换后（断言 10 个）：
```js
  it('返回 10 个开关字段，默认值符合 spec', () => {
    const o = useChartOverlays()
    expect(o.costBand).toBe(true)
    expect(o.entryLine).toBe(true)
    expect(o.volBand).toBe(true)
    expect(o.volume).toBe(true)
    expect(o.replayMarkers).toBe(true)
    expect(o.currentDecision).toBe(true)
    expect(o.deltaPane).toBe(false)
    expect(o.equityPane).toBe(false)
    expect(o.kdjPane).toBe(true)
    expect(o.rsiPane).toBe(true)
  })
```

- [ ] **Step 3.3：跑测试**

Run: `npx vitest run src/composables/__tests__/useChartOverlays.test.js`
Expected: PASS · 3 个用例（个数不变，第一个用例多 2 行断言）

- [ ] **Step 3.4：跑全量**

Run: `npm test`
Expected: PASS · 73（2 个新 indicators 文件已加） · 仍 73（修测试不增加用例数）

> 说明：Task 1 已加 5 个，Task 2 加 5 个 = 73。Task 3 不增加用例数（修原有用例的 expect 内容）。

---

## Task 4：`usePlanning.js` 加面板宽度

**Files:**
- Modify: `src/composables/usePlanning.js`

- [ ] **Step 4.1：在 activeLeftTab 之后加宽度态**

定位到（第 63 行附近）：
```js
  function toggleLeftPanel() { leftPanelOpen.value = !leftPanelOpen.value }
  function toggleRightPanel() { rightPanelOpen.value = !rightPanelOpen.value }
```

在该段后面紧接着加：

```js
  // 面板宽度（v3.2 拖宽）
  const LEFT_MIN  = 200, LEFT_MAX  = 400
  const RIGHT_MIN = 200, RIGHT_MAX = 380
  const LEFT_DEFAULT  = 280
  const RIGHT_DEFAULT = 240

  const leftPanelW  = persistedRef('lab.leftPanelW.v1', LEFT_DEFAULT)
  const rightPanelW = persistedRef('lab.rightPanelW.v1', RIGHT_DEFAULT)

  function setLeftPanelW(w) {
    leftPanelW.value = clamp(w, LEFT_MIN, LEFT_MAX)
  }
  function setRightPanelW(w) {
    rightPanelW.value = clamp(w, RIGHT_MIN, RIGHT_MAX)
  }
  function resetLeftPanelW()  { leftPanelW.value  = LEFT_DEFAULT }
  function resetRightPanelW() { rightPanelW.value = RIGHT_DEFAULT }

  function clamp(value, min, max) {
    if (!Number.isFinite(value)) return min
    return Math.min(max, Math.max(min, Math.round(value)))
  }
```

- [ ] **Step 4.2：return 同步**

在 return 块的 `toggleRightPanel,` 之后加 6 项：

```js
    leftPanelW,
    rightPanelW,
    setLeftPanelW,
    setRightPanelW,
    resetLeftPanelW,
    resetRightPanelW,
```

- [ ] **Step 4.3：更新文件顶部 JSDoc 注释**

把第 6-13 行的注释扩展加一条：

替换前：
```js
/**
 * 输入参数 + UI 选中态层
 *
 * v3.1 改造：
 *   - 删除 activeDrawer / openDrawer / closeDrawer（三栏布局不再用单例抽屉）
 *   - 新增 leftPanelOpen / rightPanelOpen / activeLeftTab + toggle 函数
 *   - 保留 input / tdpyOverride / activeFormulaId / strategyProfileList
 */
```

替换后：
```js
/**
 * 输入参数 + UI 选中态层
 *
 * v3.1 改造：删除 activeDrawer 单例；新增 leftPanelOpen / rightPanelOpen / activeLeftTab + toggle
 * v3.2 改造：新增 leftPanelW / rightPanelW（持久化）+ clamp setter + reset
 */
```

- [ ] **Step 4.4：跑既有 composables 测试**

Run: `npx vitest run src/composables/__tests__/`
Expected: PASS · 3 个用例（useChartOverlays，未涉及 usePlanning 直接测）

---

## Task 5：`labStore.js` return 同步 + 测试

**Files:**
- Modify: `src/stores/labStore.js`
- Modify: `src/stores/__tests__/labStore.test.js`

- [ ] **Step 5.1：扩 labStore.js return**

定位到 v3.1 改的"三栏面板态 + 主图叠加（v3.1）"段：

替换前：
```js
    // 三栏面板态 + 主图叠加（v3.1）
    leftPanelOpen: planning.leftPanelOpen,
    rightPanelOpen: planning.rightPanelOpen,
    activeLeftTab: planning.activeLeftTab,
    toggleLeftPanel: planning.toggleLeftPanel,
    toggleRightPanel: planning.toggleRightPanel,
    chartOverlays,
```

替换后：
```js
    // 三栏面板态 + 主图叠加（v3.1）+ 面板宽度（v3.2）
    leftPanelOpen: planning.leftPanelOpen,
    rightPanelOpen: planning.rightPanelOpen,
    activeLeftTab: planning.activeLeftTab,
    toggleLeftPanel: planning.toggleLeftPanel,
    toggleRightPanel: planning.toggleRightPanel,
    leftPanelW: planning.leftPanelW,
    rightPanelW: planning.rightPanelW,
    setLeftPanelW: planning.setLeftPanelW,
    setRightPanelW: planning.setRightPanelW,
    resetLeftPanelW: planning.resetLeftPanelW,
    resetRightPanelW: planning.resetRightPanelW,
    chartOverlays,
```

- [ ] **Step 5.2：扩"对外 API 字段齐全"用例**

`src/stores/__tests__/labStore.test.js` 第一个 it 内"三栏面板态 + 主图叠加（v3.1）"段后追加：

定位到：
```js
    expect(typeof lab.toggleLeftPanel).toBe('function')
    expect(typeof lab.toggleRightPanel).toBe('function')
    expect(lab.chartOverlays).toBeDefined()
    expect(lab.chartOverlays.costBand).toBe(true)
```

替换为（chartOverlays 段不动，在 toggleRightPanel 后插入面板宽度断言）：
```js
    expect(typeof lab.toggleLeftPanel).toBe('function')
    expect(typeof lab.toggleRightPanel).toBe('function')
    // 面板宽度（v3.2）
    expect(lab.leftPanelW).toBe(280)
    expect(lab.rightPanelW).toBe(240)
    expect(typeof lab.setLeftPanelW).toBe('function')
    expect(typeof lab.setRightPanelW).toBe('function')
    expect(typeof lab.resetLeftPanelW).toBe('function')
    expect(typeof lab.resetRightPanelW).toBe('function')
    expect(lab.chartOverlays).toBeDefined()
    expect(lab.chartOverlays.costBand).toBe(true)
```

- [ ] **Step 5.3：加 2 个新 it 用例**

在文件末尾的 `})` 之前（最后一个 `it` 之后）插入：

```js
  it('setLeftPanelW / setRightPanelW 在合法范围内生效', () => {
    const lab = useLabStore()
    lab.setLeftPanelW(320)
    expect(lab.leftPanelW).toBe(320)
    lab.setRightPanelW(280)
    expect(lab.rightPanelW).toBe(280)
  })

  it('面板宽度 clamp 上下限', () => {
    const lab = useLabStore()
    lab.setLeftPanelW(50)       // 下限 200
    expect(lab.leftPanelW).toBe(200)
    lab.setLeftPanelW(999)      // 上限 400
    expect(lab.leftPanelW).toBe(400)
    lab.setRightPanelW(50)      // 下限 200
    expect(lab.rightPanelW).toBe(200)
    lab.setRightPanelW(999)     // 上限 380
    expect(lab.rightPanelW).toBe(380)
    lab.resetLeftPanelW()
    expect(lab.leftPanelW).toBe(280)
    lab.resetRightPanelW()
    expect(lab.rightPanelW).toBe(240)
  })
```

- [ ] **Step 5.4：跑 store 测试**

Run: `npx vitest run src/stores/__tests__/labStore.test.js`
Expected: PASS · 11 个用例（原 9 + 2）

- [ ] **Step 5.5：跑全量回归**

Run: `npm test`
Expected: PASS · 73 + 2 = 75 passed

---

## Task 6：`ChartOverlayToggles.vue` 加两项

**Files:**
- Modify: `src/components/ChartOverlayToggles.vue`

- [ ] **Step 6.1：扩 TOGGLES 数组**

替换前：
```js
const TOGGLES = [
  { key: 'costBand', label: '成本锚带' },
  { key: 'entryLine', label: '入场价线' },
  { key: 'volBand', label: '波动带' },
  { key: 'volume', label: '成交量' },
  { key: 'replayMarkers', label: 'Replay 标记' },
  { key: 'currentDecision', label: '当前决策点' },
  { key: 'deltaPane', label: 'Δ 子图' },
  { key: 'equityPane', label: '权益子图' },
]
```

替换后：
```js
const TOGGLES = [
  { key: 'costBand', label: '成本锚带' },
  { key: 'entryLine', label: '入场价线' },
  { key: 'volBand', label: '波动带' },
  { key: 'volume', label: '成交量' },
  { key: 'kdjPane', label: 'KDJ 子图' },
  { key: 'rsiPane', label: 'RSI 子图' },
  { key: 'replayMarkers', label: 'Replay 标记' },
  { key: 'currentDecision', label: '当前决策点' },
  { key: 'deltaPane', label: 'Δ 子图' },
  { key: 'equityPane', label: '权益子图' },
]
```

并把第 6 行注释 "8 个主图叠加项配置" 改为 "10 个主图叠加项配置"。

- [ ] **Step 6.2：跑测试**

Run: `npm test`
Expected: PASS · 75 passed（无相关单测，但确认未破坏）

---

## Task 7：`MainChart.vue` 加 KDJ pane

**Files:**
- Modify: `src/components/MainChart.vue`

> 加 import + 2 个 series（kdjK 平均线 + kdjJ J 线）+ setData + PriceLine 阈值线 + rebalancePanes 更新。

- [ ] **Step 7.1：加 import**

定位到 MainChart.vue script setup 顶部 import 段（约第 1-12 行），在 lightweight-charts import 之后加：

```js
import { computeKDJ } from '../domain/indicators/kdj.js'
```

- [ ] **Step 7.2：applyOverlays() 中加两组 toggle**

定位到 `function applyOverlays() {` 内的 `toggle('equityZero', o.equityPane, ...)` 行之后（pane 3 权益的最后一行）加：

```js
  toggle('kdjK', o.kdjPane, () => chart.addSeries(LineSeries, {
    title: 'KD', color: 'rgba(255, 165, 0, 0.5)', lineWidth: 1,
    priceLineVisible: false, lastValueVisible: false,
  }, 4))
  toggle('kdjJ', o.kdjPane, () => chart.addSeries(LineSeries, {
    title: 'J', color: '#4e4e4e', lineWidth: 2,
    priceLineVisible: false, lastValueVisible: false,
  }, 4))
  if (series.kdjJ && !series.kdjJ.__hlinesInstalled) {
    series.kdjJ.__hlinesInstalled = true
    series.kdjJ.createPriceLine({ price: 100, color: 'rgba(255,0,0,0.3)',  lineWidth: 1, lineStyle: LineStyle.Dashed, axisLabelVisible: false })
    series.kdjJ.createPriceLine({ price: 0,   color: 'rgba(0,167,6,0.3)', lineWidth: 1, lineStyle: LineStyle.Dashed, axisLabelVisible: false })
  }
```

> 注：`__hlinesInstalled` 标记避免每次 applyOverlays 都重建 PriceLine。lw-charts 在 series 被 removeSeries 时会自动 GC PriceLine，所以只在 series 创建时挂一次。

- [ ] **Step 7.3：rebalancePanes() 重写**

定位到 `function rebalancePanes() {`，整个函数体替换为：

替换前：
```js
function rebalancePanes() {
  // pane 0: 主图 / pane 1: 成交量 / pane 2: Δ / pane 3: 权益
  const panes = chart.panes()
  if (!panes.length) return
  // 简化策略：开启 Δ/权益时给它们空间；都关时主图最大化
  const o = props.overlays
  panes[0]?.setStretchFactor(0.62)
  if (panes[1]) panes[1].setStretchFactor(o.volume ? 0.10 : 0)
  if (panes[2]) panes[2].setStretchFactor(o.deltaPane ? 0.16 : 0)
  if (panes[3]) panes[3].setStretchFactor(o.equityPane ? 0.12 : 0)
}
```

替换后：
```js
function rebalancePanes() {
  // pane 0: 主图 / 1: 量 / 2: Δ / 3: 权益 / 4: KDJ / 5: RSI
  // （pane 顺序由 addSeries(..., paneIndex) 时第三参数决定，固定不变）
  const panes = chart.panes()
  if (!panes.length) return
  const o = props.overlays
  panes[0]?.setStretchFactor(0.50)
  if (panes[1]) panes[1].setStretchFactor(o.volume    ? 0.08 : 0)
  if (panes[2]) panes[2].setStretchFactor(o.deltaPane ? 0.10 : 0)
  if (panes[3]) panes[3].setStretchFactor(o.equityPane? 0.06 : 0)
  if (panes[4]) panes[4].setStretchFactor(o.kdjPane   ? 0.13 : 0)
  if (panes[5]) panes[5].setStretchFactor(o.rsiPane   ? 0.13 : 0)
}
```

- [ ] **Step 7.4：syncChart() 加 setData**

定位到 `function syncChart() {` 内最后的 `if (markersApi) markersApi.setMarkers(buildMarkers())` 行之前，加：

```js
  if (series.kdjK || series.kdjJ) {
    const kdj = computeKDJ(props.rows)
    if (series.kdjK) {
      series.kdjK.setData(kdj
        .map((r) => ({ time: r.date, value: r.k !== null && r.d !== null ? (r.k + r.d) / 2 : null }))
        .filter((p) => p.value !== null))
    }
    if (series.kdjJ) {
      series.kdjJ.setData(kdj
        .map((r) => ({ time: r.date, value: finiteOrNull(r.j) }))
        .filter((p) => p.value !== null))
    }
  }
```

- [ ] **Step 7.5：跑测试**

Run: `npm test`
Expected: PASS · 75 passed

> MainChart 没有针对 kdjK/kdjJ 的单测；测试通过表示没有 import 报错或语法问题。视觉验收放在 Task 10。

---

## Task 8：`MainChart.vue` 加 RSI pane

**Files:**
- Modify: `src/components/MainChart.vue`

- [ ] **Step 8.1：加 import**

在 Task 7 加的 `import { computeKDJ } ...` 后追加：

```js
import { computeRSI } from '../domain/indicators/rsi.js'
```

- [ ] **Step 8.2：applyOverlays() 加 RSI series**

在 Task 7 的 KDJ hlines `if (series.kdjJ && !series.kdjJ.__hlinesInstalled) { ... }` 之后加：

```js
  toggle('rsi', o.rsiPane, () => chart.addSeries(LineSeries, {
    title: 'RSI', color: '#2e2e2e', lineWidth: 3,
    priceLineVisible: false, lastValueVisible: false,
  }, 5))
  if (series.rsi && !series.rsi.__hlinesInstalled) {
    series.rsi.__hlinesInstalled = true
    series.rsi.createPriceLine({ price: 100, color: 'rgba(120,123,134,0.5)', lineWidth: 1, lineStyle: LineStyle.Solid, axisLabelVisible: false })
    series.rsi.createPriceLine({ price: 50,  color: 'rgba(0,0,0,0.7)',       lineWidth: 1, lineStyle: LineStyle.Solid, axisLabelVisible: false })
    series.rsi.createPriceLine({ price: 0,   color: 'rgba(120,123,134,0.5)', lineWidth: 1, lineStyle: LineStyle.Solid, axisLabelVisible: false })
  }
```

- [ ] **Step 8.3：syncChart() 加 RSI setData**

在 Task 7 的 KDJ setData 块之后加：

```js
  if (series.rsi) {
    const rsi = computeRSI(props.rows)
    series.rsi.setData(rsi
      .map((r) => ({ time: r.date, value: finiteOrNull(r.custom) }))
      .filter((p) => p.value !== null))
  }
```

- [ ] **Step 8.4：跑测试**

Run: `npm test`
Expected: PASS · 75 passed

---

## Task 9：`App.vue` 接拖手

**Files:**
- Modify: `src/App.vue`

> 加 4px 拖手 div + mousemove 节流（rAF）+ 双击重置 + CSS variable 改读 store。

- [ ] **Step 9.1：扩 import + script**

定位到 `<script setup>` 段（第 1 行起），在已有 import 之后追加：

```js
// 拖宽逻辑（v3.2）
let dragging = null
let pendingW = 0
let rafId = null

function onResizerMouseDown(side, e) {
  dragging = side
  e.preventDefault()
  document.addEventListener('mousemove', onResizerMouseMove)
  document.addEventListener('mouseup', onResizerMouseUp)
  document.body.style.cursor = 'col-resize'
  document.body.style.userSelect = 'none'
}

function onResizerMouseMove(e) {
  if (!dragging) return
  pendingW = dragging === 'left' ? e.clientX : window.innerWidth - e.clientX
  if (rafId) return
  rafId = requestAnimationFrame(flushW)
}

function flushW() {
  if (dragging === 'left')  lab.setLeftPanelW(pendingW)
  if (dragging === 'right') lab.setRightPanelW(pendingW)
  rafId = null
}

function onResizerMouseUp() {
  dragging = null
  document.removeEventListener('mousemove', onResizerMouseMove)
  document.removeEventListener('mouseup', onResizerMouseUp)
  document.body.style.cursor = ''
  document.body.style.userSelect = ''
  if (rafId) { cancelAnimationFrame(rafId); rafId = null }
}

function onResizerDblclick(side) {
  side === 'left' ? lab.resetLeftPanelW() : lab.resetRightPanelW()
}

const rootStyle = computed(() => ({
  '--left-w':  `${lab.leftPanelW}px`,
  '--right-w': `${lab.rightPanelW}px`,
}))
```

> 这段加在 `function onSetAutoProfile(v) { lab.input.autoProfile = v }` 之后即可，处于 `</script>` 之前。

- [ ] **Step 9.2：template app-root 加 :style**

替换前：
```vue
  <div
    class="app-root"
    :class="{
      'left-collapsed': !effectiveLeftOpen,
      'right-collapsed': !effectiveRightOpen,
    }"
  >
```

替换后：
```vue
  <div
    class="app-root"
    :class="{
      'left-collapsed': !effectiveLeftOpen,
      'right-collapsed': !effectiveRightOpen,
    }"
    :style="rootStyle"
  >
```

- [ ] **Step 9.3：template 加两个拖手 div**

定位到 `<div class="cols">` 内，在 `<LeftPanel ... />` 之后、`<main class="app-main">` 之前插入：

```vue
      <div
        v-if="effectiveLeftOpen"
        class="resizer resizer-left"
        title="拖动调宽，双击重置"
        @mousedown="(e) => onResizerMouseDown('left', e)"
        @dblclick="onResizerDblclick('left')"
      />
```

在 `</main>` 之后、`<RightPanel ... />` 之前插入：

```vue
      <div
        v-if="effectiveRightOpen"
        class="resizer resizer-right"
        title="拖动调宽，双击重置"
        @mousedown="(e) => onResizerMouseDown('right', e)"
        @dblclick="onResizerDblclick('right')"
      />
```

- [ ] **Step 9.4：CSS 改 grid + 加拖手样式**

定位 `.app-root` 块，把内部 `--left-w: 280px; --right-w: 240px;` 删掉（现在由 :style 注入）：

替换前：
```css
.app-root {
  width: 100vw; height: 100vh;
  display: flex; flex-direction: column; overflow: hidden;
  background: var(--bg); color: var(--ink);
  --left-w: 280px;
  --right-w: 240px;
}
.app-root.left-collapsed { --left-w: 36px; }
.app-root.right-collapsed { --right-w: 36px; }
```

替换后：
```css
.app-root {
  width: 100vw; height: 100vh;
  display: flex; flex-direction: column; overflow: hidden;
  background: var(--bg); color: var(--ink);
}
.app-root.left-collapsed { --left-w: 36px !important; }
.app-root.right-collapsed { --right-w: 36px !important; }
```

> `!important` 确保折叠态压过 `:style` 注入的 CSS 变量。

定位 `.cols` 块替换：

替换前：
```css
.cols {
  flex: 1; min-height: 0;
  display: grid;
  grid-template-columns: var(--left-w) 1fr var(--right-w);
  transition: grid-template-columns 200ms ease;
}
```

替换后：
```css
.cols {
  flex: 1; min-height: 0;
  display: grid;
  grid-template-columns: var(--left-w) auto 1fr auto var(--right-w);
  transition: grid-template-columns 200ms ease;
}
.app-root.left-collapsed .cols { grid-template-columns: 36px 1fr auto var(--right-w); }
.app-root.right-collapsed .cols { grid-template-columns: var(--left-w) auto 1fr 36px; }
.app-root.left-collapsed.right-collapsed .cols { grid-template-columns: 36px 1fr 36px; }

.resizer { width: 4px; cursor: col-resize; background: transparent; transition: background 100ms; user-select: none; }
.resizer:hover, .resizer:active { background: var(--green); }
```

- [ ] **Step 9.5：跑测试**

Run: `npm test`
Expected: PASS · 75 passed

> App.vue 没有针对拖手的单测，测试通过即表示语法 OK。

---

## Task 10：最终验收

**Files:**
- 无修改

- [ ] **Step 10.1：清空 localStorage 后启动 dev**

```bash
# 浏览器 DevTools → Application → Local Storage → 清空 lab.* 键
# 或匿名窗口
```

Run: `npm run dev`

- [ ] **Step 10.2：手动验收 spec §11**

| # | 操作 | 期望 | ✅ |
|---|---|---|---|
| 1 | 首次加载 | 三栏；左 280 / 右 240；K 下显示 量 / KDJ / RSI 三 pane | ☐ |
| 2 | 设置 tab 看 ChartOverlayToggles | 10 项 toggle，KDJ 子图 / RSI 子图 默认勾 | ☐ |
| 3 | 关 KDJ 子图 | KDJ pane 消失，主 K 占更多 | ☐ |
| 4 | 关 RSI 子图 | RSI pane 消失 | ☐ |
| 5 | KDJ J 线 | 0~100+ 区间走，颜色灰；100/0 红绿虚线参考 | ☐ |
| 6 | RSI custom 线 | -70 ~ +130 区间；中轴 50 黑实线，0/100 灰参考 | ☐ |
| 7 | 鼠标悬左面板右边缘 | cursor 变 col-resize；按下拖动 → 主图自动 resize | ☐ |
| 8 | 拖出 200/400 范围 | 宽度被 clamp | ☐ |
| 9 | 双击左拖手 | 左面板回到 280 | ☐ |
| 10 | 拖右面板 | 同上，clamp 200~380；双击回 240 | ☐ |
| 11 | 刷新 | 面板宽度持久化 | ☐ |
| 12 | 收起左面板（◀） | 拖手消失，面板缩到 36px | ☐ |
| 13 | 浏览器 < 900px | 两侧拖手消失，面板都 36px | ☐ |
| 14 | 切深色主题 | KDJ/RSI 线在深色下仍可读 | ☐ |
| 15 | `npm run verify:domain` | 通过 | ☐ |
| 16 | `npm test` | 75 passed | ☐ |
| 17 | `npx vite build` | 成功 | ☐ |

- [ ] **Step 10.3：跑全量回归**

```bash
npm test
npm run verify:domain
npm run check:data && npx vite build
```

至此 v3.2 拖宽 + KDJ/RSI 改造完成。

---

## 自审清单

### Spec 覆盖检查

| Spec 章节 | 对应 Task |
|---|---|
| §3.1 computeKDJ | Task 1 |
| §3.2 computeRSI | Task 2 |
| §4.1 usePlanning 加面板宽度 | Task 4 |
| §4.2 useChartOverlays kdjPane/rsiPane | Task 3 |
| §4.3 labStore.js return | Task 5 |
| §4.4 labStore 测试 | Task 5 |
| §5.1 App.vue 拖手 | Task 9 |
| §5.2 MainChart KDJ | Task 7 |
| §5.2 MainChart RSI | Task 8 |
| §5.3 ChartOverlayToggles | Task 6 |
| §11 验收 | Task 10 |

无遗漏。

### 占位符扫描

无 TBD / TODO / "类似 task N" 等占位符。所有代码片段完整。

### 类型一致性

- `setLeftPanelW(w)` / `setRightPanelW(w)` 在 Task 4 / 5 / 9 一致
- `leftPanelW` / `rightPanelW` 字段在 store 与组件之间一致
- pane 索引：K=0 / 量=1 / Δ=2 / 权益=3 / KDJ=4 / RSI=5 与 rebalancePanes() 一致（保留 v3 顺序，新加在末尾）
- `kdjPane` / `rsiPane` 在 useChartOverlays / MainChart / ChartOverlayToggles 三处一致
- localStorage key (`lab.leftPanelW.v1` / `lab.rightPanelW.v1`) 唯一定义在 usePlanning.js

### 风险确认

- **mousemove 高频更新**：rAF 节流到 60fps，每帧 1 次 store set + 1 次 grid 重排 + 1 次 chart.resize；性能足够
- **PriceLine 创建一次**：`__hlinesInstalled` 标记避免重复；series 被 toggle off 时 lw-charts 自动清理
- **lw-charts pane 索引递增**：先 addSeries volume(1) → delta(2) → equity(3) → kdjK(4) / kdjJ(4) → rsi(5)。pane 4 由 kdjK 创建，kdjJ 用同 4；pane 5 由 rsi 创建。
- **KDJ/RSI 起步空数据**：filter 掉 null 后传给 series，避免 lw-charts 抛错

---

## 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-05-15-lab-v3.2-resize-kdj-rsi-cc.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - 主线程派 fresh subagent 跑每个 task，task 间做两阶段 review；与 v3.1 流程一致。

**2. Inline Execution** - 当前会话直接 executing-plans 一路推到底。

**Which approach?**
