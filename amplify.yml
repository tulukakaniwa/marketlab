version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@11.1.2
        - rm -rf node_modules
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
