# Archived Pine variants

These files are frozen historical evidence. They preserve retired fixed-window and
fixed-holding formulas for audit and diff purposes only.

- `executable: false`
- `formulaAuthority: none`
- `signalAuthority: none`
- `replacement: ../../../bl-esw-pinbar-market-lab.pine`
- `archivedAt: 2026-08-09`

Do not paste these variants into TradingView for current research or alerts. The only
maintained Pine contract is the repository-root `bl-esw-pinbar-market-lab.pine`, whose
window and structural horizon are prefix-causal and formula-derived.

The repository-root files with the old names are inert compatibility stubs. They emit
no plot, signal, or alert, so an old path cannot silently restore retired semantics.
